seed: 7
total_timesteps: 8192
num_policies: 6
preference_strategy: dirichlet_extremes
preference_step: 0.25
preference_dirichlet_alpha: 1.0
save_interval_updates: 4
reseed_mode: per_preference
independent_env_per_preference: true
parallel_workers: 6
parallel_backend: process
output_dir: cmorl_minicage/outputs/paper_table_a/ours_stage1
env:
  num_envs: 8
  red_policy: bline
  remove_bugs: true
  max_episode_steps: 100
  seed: 7
model:
  hidden_size: 128
  obj_dim: 3
rollout:
  num_steps: 64
eval:
  eval_episodes: 5
  preference_step:
