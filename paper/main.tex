% ACM CCS 2026-oriented rewrite
% Main changes: security-first title/abstract/introduction, explicit threat model,
% deployment-first results narrative, mandatory CCS 2026 appendices.
\documentclass[sigconf,review,anonymous]{acmart}


\AtBeginDocument{%
  \providecommand\BibTeX{{%
    Bib\TeX}}}

% =========================
% Packages
% =========================
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{amsmath}
\usepackage{graphicx}
\usepackage{subcaption}

% =========================
% Rights / Conference info
% Keep ACM metadata for CCS-style submission.
% Replace DOI / ISBN placeholders with camera-ready values after acceptance.
% =========================
\setcopyright{acmlicensed}
\copyrightyear{2026}
\acmYear{2026}
\acmDOI{10.1145/nnnnnnn.nnnnnnn}
\acmISBN{978-x-xxxx-xxxx-x/2026/11}
\acmConference[CCS '26]{2026 ACM SIGSAC Conference on Computer and Communications Security}{November 15--19, 2026}{The Hague, The Netherlands}
\acmBooktitle{2026 ACM SIGSAC Conference on Computer and Communications Security (CCS '26), November 15--19, 2026, The Hague, The Netherlands}
\acmPrice{15.00}

% =========================
% Title
% =========================
\title{Can Autonomous Cyber Defense Yield Both Effective and Deployable Guarantees? A Constrained Multi-Objective DRL Approach}
% 真实受限的条件下，ACD是否可以实现可靠的防御 
% Yielding Deployable Guarantees for Autonomous Cyber Defense: A Constraints Multi-Objective DRL Approach

%最新的题目，有陈述句和疑问句两个版本
%Learning Critical-Asset-Aware Policy Sets for Autonomous Cyber Defense under Operational Constraints
%Can Autonomous Cyber Defense Be Both Effective and Deployable? A Constrained Multi-Objective Policy-Set Approach

% 20260422 题目
% Can Autonomous Cyber Defense Yield Both Effective and Deployable Guarantees? A Constrained Multi-Objective DRL Approach

% =========================
% Authors
% =========================
\author{Anonymous Author(s)}
\affiliation{%
  \institution{Anonymous Institution}
  \country{}}
\renewcommand{\shortauthors}{Anonymous Author(s)}

% =========================
% Abstract
% 这是一版“能直接用来继续改”的 starter abstract
% 20260419
% 1.开头还是没把背景带出来。智能化攻防对抗导致ACD需要快速地针对攻击动作计算序贯决策，特别是在xxx、xxx等现实场景的防御部署中。
% 2.实验结果有没有亮眼的数据，比如具体提升了%多少，或者有没有一些比较反直觉的结果？
% 3.摘要里面部分用词不是很准确（business impact、operational constraints），这个能不能参考一下
% =========================
\begin{abstract}
% Autonomous Cyber Defense(ACD) requires sequential decision-making under competing defensive objectives. 
% Defenders must confront attackers while preserving continuity, limiting cost, and decreasing disruption, making the problem inherently constrained and multi-objective. 
% However, most existing learning-based approaches optimize a scalarized objective or produce a single deployable policy. 
% In this paper, we formulate an autonomous defender as a constrained multi-objective reinforcement learning problem with three objectives: security effect, business impact, and defense cost. 
% We adopt a two-stage Pareto-oriented learning framework that first-phase learns an initial set of preference-diverse defense policies and then expands the policy set under operational constraints to improve the Pareto-frontier quality and deployment flexibility. 
% We evaluate both candidate-set quality and final deployment quality in CybORG++ using Pareto metrics, preference-conditioned utility, and cyber-semantic outcomes. 
% Experiment results show that our approach yields higher-quality policy sets, broader preference coverage, and stronger feasible deployment choices under strict operational constraints than scalarization-based and conditioned-policy baselines.

% %按照上述要求，新修改的摘要
% AI-enabled attack--defense interaction makes autonomous cyber defense a time-sensitive sequential decision problem: in lateral-movement containment and ransomware-style response, defenders must suppress attacker progress without disrupting critical services or exhausting response capacity. 
% Existing learning-based defenses typically collapse these trade-offs into a scalarized reward or a single operating point, which poorly matches organizations with heterogeneous tolerances for residual risk, service disruption, and response overhead. 

% We formulate blue-team defense in CybORG++ as a constrained multi-objective reinforcement learning problem with three deployment objectives: security effectiveness, service continuity, and response overhead. We learn deployable defense policy sets through a two-stage Pareto-oriented framework that initializes preference-diverse policies and expands them via constraint-aware Pareto optimization. 
% Under a common constrained protocol, our method improves hyper-volume by 62.2\% over weighted-sum scalarization, increases expected utility by 36.3 points, and reduces mean constraint violation by 38.8\% while maintaining competitive security performance. 
% These results suggest that autonomous cyber defense is better framed as constrained trade-off management over deployable policy sets than as single-policy reward maximization.

% %  20260420 熊改
% LLM-enabled cyber-attacks have posed a realistic threat in cyber-space. The autonomous cyber defense (ACD) is a promising technology in confronting AI-enabled attacks. 
% However, the deep reinforcement learning (DRL) based decision-making of ACD in preventing lateral-movement and responding ransomware in IT\&OT scenarios is a time-sensitive sequential decision problem. 
% Existing methods typically collapsed the problem into a scalarized reward or a single operating point, which ignored the disruptions of critical services or exhausting defending resource in hindering sophisticated attackers.

% This paper presents a constrained multi-objective DRL method for ACD agents by formulating the defender with 3 deployable objectives: security effectiveness, service continuity, and response overhead. 
% %括一句，细写一下自己的修改，添加了基于环境中的实际语义，多加一点
% We propose a two-stage Pareto-oriented framework that initializes preference-diverse policies and expands them via the constraint-awared Pareto optimization to yield deployable policy sets in ACD.
% % 因为摘要后面可能不能修改了，所以现在里面的实验结果最好确认好，别修改后对不上
% By implemented in the CybORG++ simulators, our method improves hyper-volume by 62.2\% over weighted-sum scalarization, increases expected utility by 36.3 points, and reduces mean constraint violation by 38.8\% while maintaining competitive security performance. 
% These experiments show that a constrained trade-off management over multi-objective policy sets can enable the ACD agent with more deployable guarantees than straightforward single-policy reward maximization.
% %最后进行升华，升华到一类的领域问题


% %20260422在上面基础上最新修改的摘要
% LLM-enabled cyber attacks pose a realistic threat in cyberspace, and the autonomous cyber defense (ACD) is a promising technology for confronting AI-enabled attacks.
% However, deep reinforcement learning (DRL)-based decision-making for ACD in preventing lateral-movement and responding ransomware in IT\&OT scenarios is a time-sensitive sequential decision problem.
% Existing methods typically collapse the problem into a scalarized reward or a single operating point, which ignored the disruptions of critical services, exhausting defending resource, and rising critical risks in hindering sophisticated attackers.

% This paper presents a constrained multi-objective DRL method for ACD agents by formulating the defender with four deployment objectives: \emph{Security Effectiveness}, \emph{Business Continuity}, \emph{Defense Cost}, and \emph{Critical-Asset Safety}.
% Instead of learning a single reward-maximizing policy, our method constructs a deployable policy set through cyber-semantic objective design and constraint-aware Pareto expansion, so that different operational preferences can be served without retraining.
% We propose a two-stage Pareto-oriented framework that initializes preference-diverse policies and expands them via constraint-aware Pareto optimization to yield deployable policy sets for ACD.

% In enterprise cyber-range evaluations with partial observability, lateral movement, and critical assets, our method achieves $3.9\times$ higher hypervolume than weighted-sum scalarization, raises expected utility from $-161.57$ to $-41.92$, increases feasible deployment rate from $0.00$ to $0.64$, and reduces mean constraint violation from $93.54$ to $0.09$, with zero final critical compromised hosts and zero critical-impact events.
% These experiments show that constrained trade-off management over multi-objective policy sets can provide ACD agents with stronger deployment viability than straightforward single-policy reward maximization.
% More broadly, our results suggest that safety-critical autonomous response problems are better framed as deployment-aware constrained policy selection, rather than as single-policy scalar reward maximization.

% 20260422-熊改
% 今天再修改的话，在这个版本基础上进行，并且修改最好采用注释的方法，保留被修改内容。
LLM-enabled cyber attacks pose a realistic threat in cyberspace, and the autonomous cyber defense (ACD) is a promising technology for confronting AI-enabled attacks.
However, deep reinforcement learning (DRL)-based decision-making for ACD in preventing lateral-movement and responding ransomware in IT\&OT scenarios is a time-sensitive sequential decision problem.
Existing methods typically collapse the problem into a scalarized reward or a single operating point, which ignored the disruptions of critical services, exhaustion of defending resource, and rising critical risks in hindering sophisticated attackers.

This paper presents a constrained multi-objective DRL method for ACD agents by formulating the defender with four deployable objectives: \emph{Security Effectiveness}, \emph{Business Continuity}, \emph{Defense Cost}, and \emph{Critical-Asset Safety}.
Instead of maximizing a single reward policy, our method constructs a deployable policy set through cyber-semantic objectives and constraint-aware Pareto expansion, which different operational preferences can be realized without re-training.
We propose a two-stage Pareto-oriented framework that initializes preference-diverse policies and expands them via constraint-aware Pareto optimization to yield a deployable policy set for ACD.
By implemented in the CybORG++ simulators, our method achieves \textbf{$3.9\times$} higher hypervolume than weighted-sum scalarization, raises expected utility from $-161.57$ to $-41.92$, increases feasible deployment rate to $0.64$, and reduces mean constraint violation from $93.54$ to $0.09$, without critical compromised hosts and critical-impact events.
These experiments show that a constrained trade-off management over multi-objective policy sets can enable the ACD agent with more deployable guarantees than straightforward single-policy reward maximization.

\end{abstract}

    %Network Security track
%This paper is best suited for the Network Security track. It addresses autonomous cyber defense (ACD) for enterprise networks, where a blue-team agent must respond to lateral movement, service-impacting attack behavior, and attempts to reach critical assets under partial observability. The core security question is not the security or privacy of an ML model, but whether an autonomous network defender can provide deployable response choices that suppress attacker progress while respecting operational constraints. We therefore use DRL as a mechanism for ACD, not as the object of study. The paper formulates blue-team defense as a constrained four-objective problem—Security Effectiveness, Business Continuity, Defense Cost, and Critical-Asset Safety—and learns a Pareto policy set selected at deployment time under different organizational preferences. The threat model and evaluation are grounded in a controlled CybORG++ enterprise cyber-range setting with partial observations, standardized red-team behavior, lateral-movement paths, and critical hosts. The actionable insight is that scalarized single-policy defenses can hide unacceptable service disruption or critical-asset risk, while constrained policy sets expose deployable trade-offs and support critical-asset-aware containment decisions under operational constraints.

% 20260422 Track Justification Statement 熊改
% 参考下面的内容，不要用大模型直接生成或者翻译，好多用词都不准确。
% This paper is best suited for the track of Network Security. It addresses Autonomous Cyber Defense (ACD) for typical enterprise scenarios, where a defense agent must respond to APT attacks, such as exploitation of vulnerability, lateral movement, service-impacting, and attempts to reach critical assets, under partial observability. The core security question is not the security or privacy of an ML model, but whether an ACD agent can yield deployable response choices that interrupt attacking progress while complying with operational constraints. Therefore, we employ Deep Reinforcement Learning (DRL) as the decision-making mechanism for ACD. The paper formulates the ACD agent as a constrained multi-objective problem with 4 diverse metrics—Security Effectiveness, Business Continuity, Defense Cost, and Critical-Asset Safety—and generates a Pareto policy set selected at deployment time under different preferences. Experiments and evaluations are implemented in the CybORG++ simulator. The insight is that existing DRL methods with scalarized single-policy can hide unacceptable service disruption or critical-asset risk, while constrained policy sets reach deployable trade-offs and support critical-asset-aware decisions under operational constraints.

% =========================
% CCS Concepts
% =========================
\begin{CCSXML}
<ccs2012>
 <concept>
  <concept_id>10002978.10002991.10002994</concept_id>
  <concept_desc>Security and privacy~Intrusion/anomaly detection and malware mitigation</concept_desc>
  <concept_significance>500</concept_significance>
 </concept>
 <concept>
  <concept_id>10010147.10010257.10010293.10010294</concept_id>
  <concept_desc>Computing methodologies~Reinforcement learning</concept_desc>
  <concept_significance>500</concept_significance>
 </concept>
</ccs2012>
\end{CCSXML}

\ccsdesc[500]{Security and privacy~Intrusion/anomaly detection and malware mitigation}
\ccsdesc[500]{Computing methodologies~Reinforcement learning}

% =========================
% Keywords
% =========================
\keywords{autonomous cyber defense,
constrained multi-objective optimization,
deep reinforcement learning}
% 关键词中不要有blue-team automation
% 20260426 看一下其他文章怎么写关键词的，细节的内容不要，把大的方法总结出来


\begin{document}

\maketitle

% 正文里面除了最后实验的CybORG++的内容，也不要出现blue-team这个词，可以用ACD agent替换，因为我们是研究的一个大类的问题。而且正文里面除了最后实验也不要出现CybORG++和CybORG++的描述和内容，我们只是最后在CybORG++里面做了一个实现，整个问题不是去研究CybORG++的环境是什么样的，而是从典型场景下的问题出发，归纳总结出方法与技术，最后在CybORG++中进行了实验与评估。


%给方法起了一个名字DA-CPSL: Defense-Aware Constrained Policy-Set Learning

% 20260426 现在正文里面的用词为什么没有和摘要里面的用词对应？好多用词还是不准确。dwell， operational setting， replay-level semantic audit， evaluation protocol，这都不对

\section{Introduction}
\label{sec:introduction}

The modern enterprise network often suffers from APT attacks, in which attackers with an initial foothold conduct discovery, execute compromises, make lateral movements, and eventually disrupt business or operational processes.
AI-driven ATP attacks pose a realistic threat with the raising reasoning capabilities of LLMs.
Autonomous cyber defense (ACD), as an AI-powered defense technology, is increasingly being studied as a way to shorten defenders' response loops confronting sophisticated attacks.
Prior works on ACD have emphasized deep reinforcement learning (DRL) as the decision-making mechanism in a partially observable simulator for training an agent ~\cite{vyas2023automated,palmer2023drlsurvey,standen2021cyborg}.
However, an ACD agent requires effective and deployable guarantees in enterprise networks with critical IT and OT services.
Defensive actions are operational interventions that disrupt services, consume resources, or create unacceptable exposure around critical assets.
In IT/OT-adjacent environments, this distinction is especially important because operational technology security must account for performance, reliability, and safety requirements in addition to ordinary cybersecurity objectives~\cite{nist_sp80082r3}.

This paper proposes a constrained multi-objective model for ACD rather than a single scalar-reward maximization problem.
First, real response actions induce competing objectives: containment, business continuity, response burden, and critical-asset protection cannot always be improved simultaneously. 
Second, security and operational preferences are not fixed. 
A defender needs a continuity-preserving policy in an incident phase and a more aggressive containment policy in another. 
Third, strict limits cannot be reduced to preference weights: a policy that has high average utility may still be unacceptable if it causes excessive disruption or allows critical-asset compromise.
Although, existing DRL-based ACD methods have demonstrated useful progress in cyber ranges and reward shaping~\cite{applebaum2022bridging,foley2022autonomous,bates2023reward}, they commonly focus on learning a single operating point or improving average return, leaving less support for policy-set selection under explicit operational limits.
% 20260426 引用多一些顶刊顶会

By formulating the ACD as a constrained multi-objective decision-making problem, the defender operates under partial observability and selects host-level actions from defender-visible information. 
The APT attacker is modeled in the ATT\&CK framework with several phases to compromise an enterprise network. 
The multiple objectives include 4 operational metrics: \emph{Security Effectiveness}, \emph{Business Continuity}, \emph{Defense Cost}, and \emph{Critical-Asset Safety}. 
And the output of strategies becomes an archive of candidate policies with an assignment rule that selects only among policies satisfying strict operational limits.
% 20260426 introduction里面具体方法的过程和实验结果不需要写这么多，而是要把为什么要用这个算法的原因和怎么去设计这个算法说清楚，具体怎么做的要在后面详细说明。
% 第一句，传统经典方法为什么不行，有什么问题，所以要考虑从2个阶段来求解策略集合
% 第二句，整体说一下2个阶段是什么，之间是什么关系，分别完成了什么工作
% 第三句，这个算法怎么和ACD问题适配的
% 下面的三段内容重新梳理，合成一段话

We propose \emph{Defense-Aware Constrained Policy-Set Learning}
(DA-CPSL). 
DA-CPSL learns an evaluated archive of defense policies in two stages. 
Stage~1 trains preference-diverse initial policies over the four objectives. 
Stage~2 extends selected archive policies toward individual objective directions while preserving other objectives and enforcing operational acceptance constraints. 
Each candidate is evaluated not only by objective returns, but also by operational violation summaries and replay-level semantic audit variables. At assignment time, DA-CPSL selects a policy from the acceptable Pareto set under an operator preference. 
If no candidate satisfies the specified limits, the assignment is marked infeasible rather than forcing a high-utility but unacceptable policy. The archive-construction structure is inspired by policy-set multi-objective reinforcement learning, including recent two-stage Pareto-front discovery methods~\cite{roijers2013survey,hayes2022practical,liu2025cmorl},
but DA-CPSL changes the candidate representation, acceptance layer, and
assignment rule for autonomous defense.

We evaluate DA-CPSL in a controlled enterprise cyber-range
instantiation. The evaluation uses CybORG++ Scenario2, which builds on
the CybORG line of environments for autonomous cyber agents and provides
partial observations, post-compromise attacker progression, host-level
defensive actions, and critical-asset compromise semantics
~\cite{standen2021cyborg,emerson2024cyborgpp}. We use the cyber range as
an experimental realization of the threat model, not as the source of
the problem definition. Our evaluation separates three forms of evidence:
objective-space archive quality, operational assignment under shared
hard limits, and replay-level critical-risk semantics.

Across three seeds, DA-CPSL produces the strongest four-objective
policy archive among the evaluated methods. Compared with weighted-sum
scalarization, it achieves \(3.9\times\) higher hypervolume, improves
expected utility from \(-161.57\) to \(-41.92\), increases the
feasible-assignment rate to \(0.64\), and reduces mean constraint
violation from \(93.54\) to \(0.09\). Replay-level semantic audit shows
no observed critical breach or persistent critical breach for the
selected DA-CPSL policy under the fixed replay protocol, while
near-miss trajectories remain visible. These results support a bounded
claim: DA-CPSL improves pre-critical containment under the evaluated
threat model and cyber-range protocol, but it does not provide formal
safety guarantees, production-readiness guarantees, or elimination of
all cyber risk.

% 部分用词要参照修改
The main contributions are as follows:
\begin{itemize}
    \item Formulating the ACD as a constrained multi-objective problem, the model separates trade-offs of objective, constraints of acceptance, and assignments of preference-conditioned to yield deployable guarantees.
    \item Introducing semantics of security as the first-class objective for critical-asset
     in ACD deploying in the enterprise network, we define it to describe how a sophisticated attack can break into the critical system.
    \item Proposing the DA-CPSL algorithm to calculate the Pareto-front, we generate a defense-aware constrained policy-set from DRL training that constructs an evaluated archive, extends policies through acceptance-aware Pareto extension, and assigns policies only from the acceptable Pareto set.
\end{itemize}
% 20260426 加一段本文后续章节的安排

\section{Problem Setting and Threat Model}
\label{sec:problem-threat}

% 20260426 啥叫Operational Setting?
\subsection{Problem Setting}
\label{subsec:operational-setting}

The Stuxnet and cyber-attacks on the Ukrainian power grid have shown that confrontations in cyberspace are becoming more.
Deploying ACD for a typical enterprise network with IT/OT zones, we model the scenario including ordinary clients, business-relevant servers, and high-value OT assets, where adversarial actions will make unacceptable impact~\cite{nist_sp80082r3}. 
Therefore, we mainly focus on the cyber-defend in the IT/OT environment rather than physical-process control.

This setting is inherently multi-objective. A defensive response that improves containment may interrupt a business service, consume scarce response capacity, or introduce operational side effects if applied too aggressively.
Conversely, delaying intervention may preserve short-term continuity and reduce immediate response burden, but it may allow an attacker foothold to deepen toward critical assets.
Therefore, the defender cannot be characterized by average rewards simply, it must manage diverse competing objectives under hard constraints with operational acceptance .

The main problem is the decision-making for ACD in the typical enterprise network with constrained multi-objectives. 
The ACD agent can precept partial-observable information and generate optimal strategies under diverse preferences and strict limits. 
% The system boundary is the decision layer of autonomous defense: the defender selects response actions from defender-visible observations, and the learned archive supports policy selection under operational preferences and hard limits. We do not design enterprise monitoring or security-orchestration infrastructure, nor do we model physical-process control.

% 说了多少次了，除了实验部分不要说CybORG！！！！
% 这个图不要直接用CybORG的，自己画一下，划分3个区域，把攻防过程也体现出来
\begin{figure*}[t]
    \centering
    \includegraphics[width=\textwidth]{images/Cyber-range topology.png}
    \caption{\textbf{CybORG++ Scenario2 topology.}
    Red arrows summarize the B\_lineAgent progression from user foothold to the critical operational server. The blue dashed region denotes the defender's partial observation and host-level response scope. The highlighted server is the critical asset used for Critical-Asset Safety and semantic audit.}
    \label{fig:cyborg-topology}
\end{figure*}

% 这段按照后面修改后的图进行梳理，主要是按图说话。
Figure~\ref{fig:cyborg-topology} visualizes the topology of typical enterprise networks and attack-defense confrontations in this work. 
The topology contains user, enterprise, and operational subnets separated by routing and firewall/ACL components. 
The red path abstracts the post-compromise progression of the baseline attacker from an initial user-side foothold, through the enterprise subnet, toward the critical operational server.
The blue defender operates under partial observability and issues host-level response actions based on defender-visible telemetry.
The figure is intended to clarify the threat-model semantics; the formal decision process and objective definitions are given below.

\subsection{Threat Model}
\label{subsec:threat-model}

\paragraph{Adversary.}
The adversary is modeled by the ATT\&CK framework, which obtains an initial foothold in the enterprise network and attempts to compromise high-value or critical assets in IT/OT subnets.~\cite{vyas2023automated,palmer2023drlsurvey}.
% 20260427 三个阶段的名字修改完毕了，暂时先用这3个名字
We model the whole attacking process into 3 phases. 
In the \emph{foothold-establishing} phase, the attacker has compromised non-critical assets in the user-subnet to support further lateral movements.
In the \emph{pre-critical compromising} phase, some assets in the service-subnet have been compromised, and the compromising process creates exposures of critical-asset in the operation-subnet.
In the \emph{critical-breaching} phase, at least one critical asset in the operational subnet is compromised.

% 20260427 下面这一段内容不清楚要说什么，建议简化一下，只突出我们关注的（CybORG里面有的）。现在写的感觉文中的模型很简单，建议看一下CybORG的文章，把里面对攻击过程的描述复用一下。结合拓扑图说明的的内容，要从具体的流程角度说的清楚一些，看一下ATT&CK框架里面怎么说的，把哪些专有名词放过来，比如建立立足点可以通过phishing，后面通过漏洞利用和横向移动最好也找一个具体的TTP名称，这样看着模型是比较真实。
We focus on post-compromise progression rather than the full intrusion lifecycle.
We do not model initial access, phishing, exploit generation, supply-chain compromise, physical sabotage, or an online adaptive attacker that observes and co-learns against the current defense policy.
These exclusions keep the threat model centered on operational response to attacker progression.

The red trajectory in Figure~\ref{fig:cyborg-topology} summarizes the attacker progression used throughout the paper: an initial foothold in the user-facing part of the network can develop into enterprise foothold, then pre-critical exposure, and finally realized compromise of the critical operational server.
We use this abstraction to define critical-risk semantics, not to model the full intrusion lifecycle.

\paragraph{Defender.}
The ACD agent operates under partial observability. At time \(t\), the defender receives an observation \(o_t\) derived from defender-visible telemetry, alerts, and host-level status.
The defender does not observe the full latent cyber state, the attacker's internal plan, or future attacker actions.

The defender selects host-level response actions such as investigation, containment, removal, restoration, deception, or other defensive interventions.
These actions differ in both security effect and operational side effect.
For example, restoration may remove attacker presence but disrupt a business service; deception may improve containment but consume response resources; conservative response may preserve short-term continuity but allow an attacker foothold to deepen.
We assume that the defender's sensing and actuation interfaces are available within the modeled decision process; attacks that compromise these interfaces are outside this threat model.

The dashed blue scope in Figure~\ref{fig:cyborg-topology} denotes the defender's observation and response interface. It should not be interpreted as privileged access to the latent cyber state. The learned policy receives only defender-visible observations and selects host-level defensive actions.

\paragraph{Critical assets and security semantics.}

In an enterprise network with IT/OT-converged operators, critical assets include systems that support remote access, engineering workflows, historical data, and other high-value services. And these systems use cyber entities, such as PCs, servers, PDAs, and other digital platforms, to provide services, which contains potential cybersecurity vulnerabilities.

Let \(\mathcal{E}\) denotes the set of cyber entities, \(\mathcal{K}\subseteq\mathcal{E}\) denotes the set of critical-asset set, and \(C_t\subseteq\mathcal{E}\) denotes the set of compromised entities at time \(t\). Then the indicator of critical-breaches can be denoted as:
\begin{equation}
b_t^{\mathrm{crit}}
=
\mathbb{1}\{C_t\cap\mathcal{K}\neq\emptyset\}.
\label{eq:threat-critical-breach}
\end{equation}
And the indicator of pre-critical exposures can be denoted as:
\begin{equation}
b_t^{\mathrm{pre}}
=
\mathbb{1}
\left\{
C_t\cap\mathcal{K}=\emptyset
\;\wedge\;
\rho(C_t,\mathcal{K})>0
\right\},
\label{eq:threat-precritical}
\end{equation}
where \(\rho(C_t,\mathcal{K})\) is a policy-independent filter function that measures potential exposures around critical assets. Thus, \(b_t^{\mathrm{pre}}=1\) denotes a pre-critical state, and \(b_t^{\mathrm{crit}}=1\) denotes existent critical breaches.

These indicators are not additional observations used for decision-making. 
Meanwhile, they are used to define security semantics for generalized safety of critical assets under cyber-attacks, and evaluate whether a strategy with high-score in average rewards can exhibit unacceptable risks in critical assets. Detailed definitions are formalized in Section~\ref{sec:formulation} and Appendix~\ref{app:critical-audit}.

\subsection{Operational Preferences and Scope}
\label{subsec:operational-preferences-scope}

Organizations differ in how they trade off containment, service
continuity, response burden, and critical-asset protection. We represent
these differences through operational preferences over four objectives:
\emph{Security Effectiveness}, \emph{Business Continuity},
\emph{Defense Cost}, and \emph{Critical-Asset Safety}. Preferences,
however, are not hard limits. A preference specifies how an organization
ranks acceptable trade-offs, whereas a hard operational limit specifies
behavior that must be rejected regardless of scalar utility.

Under this threat model, the research object is not a universally
optimal autonomous defender. It is a finite archive of candidate defense
policies whose members expose different trade-offs among containment,
continuity, response cost, and Critical-Asset Safety, together with a
constrained selection rule that chooses an acceptable policy under
operator-specified preferences and hard operational limits. If no
candidate satisfies the specified limits, the selection should be marked
infeasible rather than forcing an unacceptable policy choice.

Our claims are limited to constrained policy-set learning and policy
selection under the stated threat model. The operational limits abstract
business-disruption tolerance, response-resource tolerance, and
critical-asset acceptance criteria; they do not replace
organization-specific approval, escalation, legal, or OT
safety-engineering processes. We do not claim formal safety guarantees,
readiness for unsupervised production operation, or elimination of all
cyber risk.


\section{Formal Model of Constrained Multi-Objective ACD}
\label{sec:formulation}

As described in Section~\ref{sec:problem-threat}, our model can be formalized as a finite-horizon, partially observable, constrained multi-objective decision-making problem of ACD. 
By separated objective trade-offs, strict constraints, and preference-conditioned policy, this model can transform a single scalar reward into a constrained multi-objective DRL-based ACD~\cite{roijers2013survey,hayes2022practical,altman2021cmdp}.

\subsection{Partially Observable Defense Process}
\label{subsec:defense-process}

We model autonomous defense as a finite-horizon partially observable multi-objective decision process:
\begin{equation}
\mathcal{M}
=
\langle
\mathcal{S},
\mathcal{O},
\mathcal{A},
P,
Z,
\mu_0,
\mathbf{r},
\gamma,
T
\rangle .
\label{eq:defense-process}
\end{equation}
Here, \(s_t\in\mathcal{S}\) is the latent cyber state,
\(o_t\in\mathcal{O}\) is the defender-visible observation, and
\(a_t\in\mathcal{A}\) is a defensive action. The observation model is
\(Z(o_t\mid s_t)\), \(\mu_0\) is the initial-state distribution,
and the transition kernel
\(P(s_{t+1}\mid s_t,a_t)\) captures attacker progression, defensive
action effects, and service-state evolution.

The defense policy acts on observation histories rather than on the
latent cyber state. We write the policy as:
\begin{equation}
a_t \sim \pi(\cdot\mid h_t),
\qquad
h_t=(o_0,a_0,o_1,a_1,\ldots,o_t),
\label{eq:history-policy}
\end{equation}
where \(h_t\) may be represented explicitly or by a recurrent policy
state. The policy is not given privileged access to the full latent
state, the attacker's internal plan, future attacker actions, or
trajectory-level audit variables.

For a policy \(\pi\), we denote the induced trajectory distribution as:
\begin{equation}
\tau \sim \mathbb{P}_{\mathcal{M}}^\pi .
\label{eq:trajectory-distribution}
\end{equation}
Audit variables derived from latent compromise state are used for reward
construction, diagnostics, and evaluation, but they are not exposed as
policy inputs.

\subsection{Operational Objectives and Critical-Asset Safety}
\label{subsec:objectives}

Each transition produces a four-dimensional reward vector:
\begin{equation}
\mathbf{r}_t
=
\left[
r_t^{\mathrm{sec}},
r_t^{\mathrm{bus}},
r_t^{\mathrm{cost}},
r_t^{\mathrm{crit}}
\right]^\top ,
\label{eq:reward-vector}
\end{equation}
corresponding to \emph{Security Effectiveness}, \emph{Business
Continuity}, \emph{Defense Cost}, and \emph{Critical-Asset Safety}. For
a policy \(\pi\), we define the expected return vector as:
\begin{equation}
\mathbf{J}(\pi)
=
\mathbb{E}_{\tau\sim\mathbb{P}_{\mathcal{M}}^\pi}
\left[
\sum_{t=0}^{T-1}
\gamma^t
\mathbf{r}_t
\right]
=
\left[
J^{\mathrm{sec}}(\pi),
J^{\mathrm{bus}}(\pi),
J^{\mathrm{cost}}(\pi),
J^{\mathrm{crit}}(\pi)
\right]^\top .
\label{eq:return-vector}
\end{equation}
All four objectives are written in maximization form. In particular,
\(J^{\mathrm{cost}}\) is a utility-transformed Defense Cost objective,
where larger values correspond to lower realized response burden.

Security Effectiveness captures general containment of attacker presence
and progression across the enterprise environment. Business Continuity
captures preservation of ordinary service operation under defensive
interventions. Defense Cost captures response burden and reliance on
costly or disruptive actions. Critical-Asset Safety captures tail-risk
behavior around the critical-asset set; it is distinct from Security
Effectiveness because it focuses on first critical hits, persistent
critical compromise, and critical dwell rather than general containment
alone.

Using the critical-breach and pre-critical indicators defined in
Section~\ref{subsec:threat-model}, we define two additional step-level
indicators for Critical-Asset Safety. The first critical-hit indicator is
defined as:
\begin{equation}
f_t^{\mathrm{crit}}
=
\mathbb{1}
\left\{
b_t^{\mathrm{crit}}=1
\;\wedge\;
\sum_{k=0}^{t-1} b_k^{\mathrm{crit}}=0
\right\}.
\label{eq:first-hit}
\end{equation}
We use the convention that an empty sum is zero, so this definition is
valid at \(t=0\). For a persistence horizon \(L\), the persistent
critical-compromise indicator is defined as:
\begin{equation}
p_{t,L}^{\mathrm{crit}}
=
\mathbb{1}
\left\{
t\geq L-1
\;\wedge\;
\sum_{k=t-L+1}^{t}
b_k^{\mathrm{crit}}
=
L
\right\}.
\label{eq:persistent-step}
\end{equation}
In evaluation, \(L\) is fixed by the semantic audit protocol.

We define the per-step Critical-Asset Safety reward as:
\begin{equation}
r_t^{\mathrm{crit}}
=
-
\lambda_{\mathrm{pre}} b_t^{\mathrm{pre}}
-
\lambda_{\mathrm{hit}} f_t^{\mathrm{crit}}
-
\lambda_{\mathrm{dwell}} b_t^{\mathrm{crit}}
-
\lambda_{\mathrm{persist}} p_{t,L}^{\mathrm{crit}},
\label{eq:crit-reward}
\end{equation}
where all coefficients are non-negative. The corresponding policy return
\(J^{\mathrm{crit}}(\pi)\) is the fourth component of
Eq.~\ref{eq:return-vector}. This objective encourages containment in the
pre-critical window before attacker progression becomes realized
critical-asset compromise. Optimizing Critical-Asset Safety does not
provide a formal safety guarantee; it provides a decision objective and
an audit signal for reducing critical-asset risk under the modeled
defense process.

For replay-level diagnostics, we use trajectory summaries such as:
\begin{equation}
B_{\mathrm{crit}}(\tau)
=
\mathbb{1}
\left\{
\sum_{t=0}^{T-1}
b_t^{\mathrm{crit}}>0
\right\},
\qquad
D_{\mathrm{crit}}(\tau)
=
\sum_{t=0}^{T-1}
b_t^{\mathrm{crit}},
\label{eq:crit-audit-summary}
\end{equation}
which indicate whether a critical breach ever occurs and how long
critical compromise persists. Additional audit summaries, including
first critical-hit time and persistent breach rate, are defined in
Appendix~\ref{app:critical-audit} and reported in the experimental
protocol. These trajectory-level variables are used for semantic audit
and event-style constraints, not as privileged policy inputs.

\subsection{Operational Acceptance Constraints}
\label{subsec:constraints}

Operational preferences are distinct from hard acceptance constraints. A
preference specifies how an operator ranks trade-offs among the four
objectives, whereas a hard constraint specifies behavior that must be
rejected regardless of scalar utility.

Let \(\Pi\) denote a finite archive of candidate defense policies. For a
policy \(\pi\in\Pi\), we define the \(m\)-th operational violation
summary as:
\begin{equation}
g_m(\pi)
=
\mathbb{E}_{\tau\sim\mathbb{P}_{\mathcal{M}}^\pi}
\left[
c_m(\tau)
\right],
\qquad
m=1,\ldots,M ,
\label{eq:constraint-expectation}
\end{equation}
where \(c_m(\tau)\geq 0\) is a trajectory-level violation function. For
event-style constraints, \(c_m(\tau)\) may be an indicator such as
\(B_{\mathrm{crit}}(\tau)\), so that \(g_m(\pi)\) is an event rate under
the modeled process. Other constraints may summarize business-service
disruption, excessive response burden, or high-disruption intervention
patterns.

For operational limits
\(\boldsymbol{\delta}=[\delta_1,\ldots,\delta_M]^\top\), we define the
acceptable archive as:
\begin{equation}
\Pi_{\mathrm{acc}}(\Pi,\boldsymbol{\delta})
=
\left\{
\pi\in\Pi:
g_m(\pi)\leq \delta_m,
\; m=1,\ldots,M
\right\}.
\label{eq:acceptable-archive}
\end{equation}
When the archive \(\Pi\) is clear from context, we write
\(\Pi_{\mathrm{acc}}(\boldsymbol{\delta})\) for brevity.

We also define the mean constraint violation as:
\begin{equation}
V(\pi;\boldsymbol{\delta})
=
\frac{1}{M}
\sum_{m=1}^{M}
\left[
g_m(\pi)-\delta_m
\right]_+ ,
\label{eq:mean-violation}
\end{equation}
where \([x]_+=\max(x,0)\). A policy with
\(V(\pi;\boldsymbol{\delta})=0\) satisfies all specified limits. In
experiments, \(g_m(\pi)\) is estimated from a fixed rollout protocol, so
operational acceptance is empirical under the stated protocol rather
than a formal guarantee over all possible executions or attacker
variants.

The acceptance layer is a hard filter, not another preference weight. A
policy can achieve high scalar utility and still be rejected if it
violates operational limits or exhibits unacceptable critical-risk
behavior.

\subsection{Acceptable Pareto Assignment}
\label{subsec:acceptable-pareto-assignment}

Because the four objectives can conflict, a single defense policy need
not be best under all operational regimes. We use Pareto dominance to
describe objective-space trade-offs~\cite{roijers2013survey,hayes2022practical}. For two policies \(\pi_a\) and
\(\pi_b\), we say that \(\pi_a\) objective-dominates \(\pi_b\), written
\(\pi_a \succ_J \pi_b\), if:
\begin{equation}
J_i(\pi_a)
\geq
J_i(\pi_b),
\qquad
\forall i\in
\{\mathrm{sec},\mathrm{bus},\mathrm{cost},\mathrm{crit}\},
\label{eq:dominance-weak}
\end{equation}
and the inequality is strict for at least one objective. Since all
objectives are in maximization form, this dominance relation has a
consistent direction.

We define the raw Pareto set induced by archive \(\Pi\) as:
\begin{equation}
\Pi_{\mathrm{P}}(\Pi)
=
\left\{
\pi\in\Pi:
\nexists \pi'\in\Pi
\text{ such that }
\pi'\succ_J \pi
\right\}.
\label{eq:raw-pareto-set}
\end{equation}
Raw Pareto optimality is not sufficient for operational selection. A
policy may be non-dominated in objective space but unacceptable if it
violates hard limits on disruption, response burden, or critical-risk
behavior.

We therefore define the acceptable Pareto set after applying the
operational acceptance filter:
\begin{equation}
\Pi_{\mathrm{P}}^{\mathrm{acc}}(\Pi,\boldsymbol{\delta})
=
\left\{
\pi\in
\Pi_{\mathrm{acc}}(\Pi,\boldsymbol{\delta}):
\nexists \pi'\in
\Pi_{\mathrm{acc}}(\Pi,\boldsymbol{\delta})
\text{ such that }
\pi'\succ_J \pi
\right\}.
\label{eq:acceptable-pareto-set}
\end{equation}
This distinction is central to autonomous defense: Pareto dominance
describes objective-space trade-off quality, whereas operational
acceptance determines whether a candidate policy is eligible for
selection at all.

Let
\begin{equation}
\mathbf{w}\in\Delta_4,
\qquad
\Delta_4
=
\left\{
\mathbf{w}\in\mathbb{R}_{\geq 0}^{4}:
\sum_{i=1}^{4} w_i=1
\right\}
\label{eq:preference-simplex}
\end{equation}
denote an operator preference over the four objectives. Because the
objectives may have different numerical scales, scalar utility is
computed on a calibrated objective vector:
\begin{equation}
\widetilde{\mathbf{J}}(\pi)
=
\mathcal{C}(\mathbf{J}(\pi)),
\label{eq:calibrated-objective}
\end{equation}
where \(\mathcal{C}\) is fixed by the evaluation protocol and is
coordinate-wise monotone. The same calibration map is used for all
methods within an evaluation protocol.

Given preference \(\mathbf{w}\) and operational limits
\(\boldsymbol{\delta}\), we define the constrained assignment rule as:
\begin{equation}
\pi^\star(\mathbf{w},\boldsymbol{\delta})
=
\arg\max_{\pi\in
\Pi_{\mathrm{P}}^{\mathrm{acc}}(\Pi,\boldsymbol{\delta})}
\mathbf{w}^{\top}
\widetilde{\mathbf{J}}(\pi).
\label{eq:constrained-assignment}
\end{equation}
If
\(\Pi_{\mathrm{P}}^{\mathrm{acc}}(\Pi,\boldsymbol{\delta})=\emptyset\),
the assignment is marked infeasible rather than forcing a policy that
violates hard operational limits. This constrained assignment is the
operational counterpart of policy-set assignment in multi-objective
reinforcement learning~\cite{vanmoffaert2014sets,zahavy2021policies,liu2025cmorl}: preferences select among acceptable policies,
while hard constraints determine which policies are acceptable in the
first place.


\section{DA-CPSL: Learning Constrained Defense Policy Sets}
\label{sec:learning}

The formulation in Section~\ref{sec:formulation} requires a learning
procedure that constructs a policy archive, not a single scalar-reward
policy. We propose \emph{Defense-Aware Constrained Policy-Set Learning}
(DA-CPSL), which learns a finite archive of defense policies and selects
policies only through the operational acceptance layer defined in
Section~\ref{subsec:acceptable-pareto-assignment}.

DA-CPSL uses a two-stage archive-construction structure inspired by
policy-set MORL~\cite{vanmoffaert2014sets,hayes2022practical}, particularly C-MORL~\cite{liu2025cmorl}, which
initializes diverse policies and extends selected policies toward
different objective directions. We use this structure as an archive
construction template, but the learning object and assignment rule are
defense-specific: every candidate is evaluated by objective returns,
operational violations, and semantic critical-risk audits, and final
assignment is performed only over the acceptable Pareto set.

Figure~\ref{fig:dacpsl-overview} gives an overview of DA-CPSL. The upper lane constructs an evaluated archive of defense policies, while the lower lane performs deployment-time operational assignment. Stage 1 trains preference-diverse initial policies over the four defense objectives. Each candidate is then evaluated as an archive record containing objective estimates, operational violation summaries, and replay-level semantic audit variables. Stage 2 expands the archive through acceptance-aware Pareto extension, where selected parent policies are improved toward individual objective directions while preserving other objectives and respecting operational limits. At deployment time, an operator preference and hard limits select a policy only from the acceptable Pareto set; if no acceptable candidate exists, the assignment is marked infeasible.

% 图里不要说CybORG
\begin{figure*}[t]
    \centering
    \includegraphics[width=\textwidth]{images/Overview of DA-CPSL.png}
    \caption{\textbf{Overview of DA-CPSL.}
    DA-CPSL constructs an evaluated archive of autonomous-defense policies rather than a single scalar-reward policy. Stage 1 trains preference-diverse initial policies over Security Effectiveness, Business Continuity, Defense Cost, and Critical-Asset Safety. Candidate policies are evaluated by objective returns, operational violation summaries, and replay-level semantic audit variables. Stage 2 performs acceptance-aware Pareto extension to expand the archive toward useful and operationally acceptable trade-offs. At deployment time, an operator preference and hard operational limits select a policy from the acceptable Pareto set; if no candidate satisfies the limits, the assignment is marked infeasible.}
    \label{fig:dacpsl-overview}
\end{figure*}

\subsection{Method Overview}
\label{subsec:dacpsl-overview}

DA-CPSL maintains a finite archive of candidate defense policies:
\[
\Pi=\{\pi_1,\ldots,\pi_{K_\Pi}\}.
\]
Each candidate policy is stored as an evaluated record:
\begin{equation}
x(\pi)
=
\left(
\pi,
\widehat{\mathbf{J}}(\pi),
\widehat{\mathbf{g}}(\pi),
\widehat{\boldsymbol{\psi}}(\pi)
\right),
\label{eq:dacpsl-record}
\end{equation}
where \(\widehat{\mathbf{J}}(\pi)\) estimates the four objective returns,
\(\widehat{\mathbf{g}}(\pi)\) estimates operational violation summaries,
and \(\widehat{\boldsymbol{\psi}}(\pi)\) records replay-level semantic
audit variables such as critical breach, persistent critical breach,
first critical-hit time, and critical dwell.

The method has two stages. Stage~1 trains an initial set of
preference-diverse defense policies. Stage~2 expands the archive by
selecting parent policies from sparse and operationally promising
regions of the archive, then extending each parent toward individual
objective directions while preserving other objectives and respecting
operational acceptance constraints. After training, the archive supports
preference-conditioned assignment without retraining. If no candidate
satisfies the specified operational limits, DA-CPSL returns
\emph{infeasible} rather than forcing an unacceptable policy choice.

\paragraph{Workflow.}
DA-CPSL proceeds as follows: (i) train initial policies under sampled
objective preferences; (ii) evaluate every candidate by objective
returns, constraint violations, and semantic audits; (iii) construct raw
and acceptable Pareto views of the archive; (iv) extend selected
candidates through acceptance-aware Pareto extension; and (v) perform
operational assignment using Eq.~\ref{eq:constrained-assignment}.

\subsection{Stage 1: Preference-Diverse Initialization}
\label{subsec:dacpsl-stage1}

Stage~1 constructs an initial archive by training defense policies under
sampled objective preferences. Let
\[
\mathcal{W}_{\mathrm{init}}\subset\Delta_4
\]
denote the initial preference set over Security Effectiveness, Business
Continuity, Defense Cost, and Critical-Asset Safety. For each
\(\mathbf{w}\in\mathcal{W}_{\mathrm{init}}\), DA-CPSL trains a policy by
maximizing the scalarized return~\cite{vanmoffaert2013scalarized,yang2019generalized}:
\begin{equation}
J_{\mathbf{w}}(\pi)
=
\mathbb{E}_{\tau\sim\mathbb{P}_{\mathcal{M}}^\pi}
\left[
\sum_{t=0}^{T-1}
\gamma^t
\mathbf{w}^{\top}\mathbf{r}_t
\right].
\label{eq:stage1-scalarized-return}
\end{equation}

The preference vector is used only to induce diversity during
initialization. Once a policy is trained, it is stored as a
preference-independent candidate and can later be evaluated under any
operator preference. After training, each initial policy is evaluated
under the fixed evaluation protocol to estimate
\(\widehat{\mathbf{J}}(\pi)\), \(\widehat{\mathbf{g}}(\pi)\), and
\(\widehat{\boldsymbol{\psi}}(\pi)\). The result is an initial evaluated
archive \(\Pi_{\mathrm{init}}\).

This initialization stage provides diverse starting points, but it does
not by itself guarantee operational acceptability. A policy trained under
a particular preference may still violate business-disruption, response
burden, or critical-risk limits. Therefore, every policy must pass the
acceptance filter before it can be used for operational assignment.

\subsection{Stage 2: Acceptance-Aware Pareto Extension}
\label{subsec:dacpsl-stage2}

Stage~2 expands the archive by improving selected parent policies along
individual objective directions. Let
\[
\mathcal{I}
=
\{
\mathrm{sec},
\mathrm{bus},
\mathrm{cost},
\mathrm{crit}
\}
\]
denote the objective index set. Given a parent policy \(\pi_r\) and a
target objective \(\ell\in\mathcal{I}\), the extension step seeks a child
policy that improves objective \(\ell\) while avoiding unacceptable
regression on the other objectives and satisfying operational acceptance
constraints.

\paragraph{Parent selection.}
At each extension round, DA-CPSL computes archive views using the
definitions in Section~\ref{subsec:acceptable-pareto-assignment}. When
\(\Pi_{\mathrm{P}}^{\mathrm{acc}}(\Pi,\boldsymbol{\delta})\) is
non-empty, parent policies are selected from this acceptable Pareto set.
If no acceptable Pareto candidate exists early in training, the method
may use the raw Pareto view for exploration, but such candidates remain
ineligible for operational assignment unless they later pass the
acceptance filter.

Within the candidate source set, DA-CPSL encourages objective-space
coverage~\cite{deb2002nsga2,liu2025cmorl} by prioritizing sparse regions of the calibrated objective
space. Extreme candidates for each objective are retained when possible,
and remaining parent slots are assigned to candidates that improve
frontier coverage while exhibiting lower constraint violation and lower
critical-risk audit summaries. Thus, parent selection treats diversity
and operational acceptability as separate requirements.

\paragraph{Extension objective.}
Let
\[
\widetilde{\mathbf{J}}(\pi)
=
\mathcal{C}(\mathbf{J}(\pi))
\]
be the calibrated objective vector from
Eq.~\ref{eq:calibrated-objective}. 
For a parent-relative preservation multiplier \(\eta_i>0\), we define the
non-target objective threshold as:
\begin{equation}
d_i(\pi_r;\eta_i)
=
\eta_i\,
\widetilde{J}_i(\pi_r),
\qquad i\in\mathcal{I},\; i\neq \ell .
\label{eq:parent-relative-threshold}
\end{equation}
The multiplier is applied on the calibrated objective scale; in our
evaluation this calibrated scale is non-negative, and values slightly
above one impose a strict non-regression margin on non-target objectives.
The idealized extension problem is defined as:
\begin{align}
\pi_{\ell}^{+}
\in
\arg\max_{\pi}
&\quad
\widetilde{J}_{\ell}(\pi)
\label{eq:ideal-extension}
\\
\mathrm{s.t.}
&\quad
\widetilde{J}_{i}(\pi)
\geq
d_i(\pi_r;\eta_i),
\qquad i\in\mathcal{I},\; i\neq \ell ,
\nonumber\\
&\quad
g_m(\pi)
\leq
\delta_m,
\qquad m=1,\ldots,M .
\nonumber
\end{align}
The first constraint family prevents extension toward one objective from
destroying the other objectives. The second constraint family enforces
operational acceptance limits.

In implementation, Eq.~\ref{eq:ideal-extension} is optimized using a
policy-gradient surrogate with return-preservation terms and violation
penalties~\cite{schulman2017ppo,achiam2017cpo,liu2020ipo}. The surrogate is a training mechanism, not the final
acceptance decision. After each extension update, the child policy is
re-evaluated under the fixed evaluation protocol to obtain
\(\widehat{\mathbf{J}}\), \(\widehat{\mathbf{g}}\), and
\(\widehat{\boldsymbol{\psi}}\). Operational eligibility is determined
from these post-update evaluations. Appendix~\ref{app:dacpsl-details}
provides the detailed surrogate and algorithmic workflow.

\subsection{Archive Update and Semantic Acceptance}
\label{subsec:dacpsl-archive-update}

A child policy is not considered acceptable merely because it improves a
target objective or increases raw archive quality. DA-CPSL updates the
archive using evaluated candidate records and separates three views of
the archive.

\paragraph{Raw archive.}
The raw archive stores evaluated candidates and their records
\(x(\pi)\). It supports analysis of candidate-set quality and retains
policies that may be useful for later extension, even if they are not
acceptable under the current operational limits.

\paragraph{Acceptable archive.}
The acceptable archive
\(\Pi_{\mathrm{acc}}(\Pi,\boldsymbol{\delta})\) contains candidates that
satisfy the operational acceptance constraints in
Eq.~\ref{eq:acceptable-archive}. A candidate that violates these limits
may remain in the raw archive, but it is not eligible for operational
assignment under the corresponding \(\boldsymbol{\delta}\).

\paragraph{Semantic audit.}
The audit vector \(\widehat{\boldsymbol{\psi}}(\pi)\) records
security-relevant trajectory behavior. DA-CPSL uses these summaries to
identify candidates that appear strong in average return but exhibit
unacceptable critical-risk behavior, such as realized critical breach,
persistent critical breach, excessive critical dwell, or severe near-miss
behavior in the pre-critical window.

The archive update rule can be summarized as:
\begin{equation}
\Pi
\leftarrow
\Pi
\cup
\{
\pi_{\ell}^{+}
\},
\qquad
x(\pi_{\ell}^{+})
=
\left(
\pi_{\ell}^{+},
\widehat{\mathbf{J}}(\pi_{\ell}^{+}),
\widehat{\mathbf{g}}(\pi_{\ell}^{+}),
\widehat{\boldsymbol{\psi}}(\pi_{\ell}^{+})
\right).
\label{eq:archive-update}
\end{equation}
The raw and acceptable Pareto sets are then computed as views of this
evaluated archive. This design avoids prematurely deleting a raw
dominated policy that may remain useful if the policies dominating it
fail operational acceptance constraints.

\subsection{Operational Assignment and Complexity}
\label{subsec:dacpsl-assignment}

After archive construction, operational adaptation is performed by
assignment rather than retraining. Given an operator preference
\(\mathbf{w}\in\Delta_4\) and operational limits
\(\boldsymbol{\delta}\), DA-CPSL computes the acceptable Pareto set
\[
\Pi_{\mathrm{P}}^{\mathrm{acc}}(\Pi,\boldsymbol{\delta})
\]
and applies the constrained assignment rule in
Eq.~\ref{eq:constrained-assignment}. If the acceptable Pareto set is
empty, DA-CPSL reports the assignment as infeasible.

This behavior is deliberate. A high-utility policy that violates hard
operational limits is not treated as an acceptable substitute. For
diagnostics, the system may report the least-violating candidate, but it
is not counted as a successful operational assignment.

Let \(n=|\mathcal{I}|\), \(K_{\mathrm{ext}}\) be the number of extension
rounds, and \(N_{\mathrm{parent}}\) be the number of parent policies
selected per round. Under a fixed per-extension training budget, the
number of extension optimization calls scales as:
\begin{equation}
O(n K_{\mathrm{ext}} N_{\mathrm{parent}}).
\label{eq:dacpsl-complexity}
\end{equation}
The additional cost relative to generic policy-set learning comes from
candidate evaluation, operational constraint estimation, and semantic
audit. These costs are necessary for distinguishing high-return policies
from operationally acceptable defense policies.

DA-CPSL therefore produces not only a set of high-return policies, but
also an evaluated archive whose candidates can be filtered by hard
operational limits, audited for critical-risk behavior, and assigned
under changing operator preferences.


\section{Experimental Setup and Evaluation Protocol}
\label{sec:evaluation-setup}

This section describes how we instantiate the threat model and
formulation in a controlled cyber-range evaluation. The goal of the
evaluation is not to show that an autonomous defender is ready for
unsupervised production operation. Instead, we test whether DA-CPSL
constructs a stronger defense policy archive, whether its assignments
satisfy shared operational limits, and whether the selected policies
exhibit safer replay-level critical-asset semantics under the evaluated
protocol.

\subsection{Evaluation Questions}
\label{subsec:eval-questions}

We organize the evaluation around four research questions.

\paragraph{RQ1: Archive quality.}
Does DA-CPSL construct a stronger four-objective defense policy archive
than scalarized or non-acceptance-aware alternatives? This question
evaluates objective-space candidate quality using policy-set metrics
such as hypervolume, expected utility, coverage, and the number of
distinct assigned policies.

\paragraph{RQ2: Operational acceptability.}
Does DA-CPSL produce more acceptable policy assignments under shared
operational limits? This question evaluates feasible-assignment rate,
mean constraint violation, and the realized security, business,
response-burden, and critical-asset outcomes of assigned policies.

\paragraph{RQ3: Replay-level cyber semantics.}
Do selected DA-CPSL policies contain attacker progression before it
crosses from pre-critical exposure into realized critical breach? This
question evaluates trajectory-level semantic audit metrics, including
ever-critical-breach rate, persistent-critical-breach rate, critical
dwell, first critical-hit time, and pre-critical response behavior.

\paragraph{RQ4: Component necessity.}
Which components are necessary for the observed behavior? This question
tests whether Stage~2 extension, acceptance-aware shaping, and
Critical-Asset Safety are required for both archive quality and
operational acceptability.

\subsection{Cyber-Range and Objective Instantiation}
\label{subsec:eval-cyber-range}

We instantiate the threat model in the CybORG++ Scenario2~\cite{standen2021cyborg,emerson2024cyborgpp} topology illustrated in Figure~\ref{fig:cyborg-topology}. We instantiate the threat model in CybORG++ Scenario2, a controlled
enterprise cyber range for autonomous defense. The instantiation provides
partial observations, host-level response actions, post-compromise
attacker progression, business-impacting defensive side effects, and
critical-asset compromise semantics. The cyber range is used as an
experimental realization of the problem class in
Section~\ref{sec:problem-threat}; it is not the source of the threat
model itself.

Unless otherwise stated, all experiments use the standard fixed baseline
attacker process, a finite horizon of \(T=100\), vector observations, and
the same Scenario2 configuration. The fixed attacker process is a
deliberate evaluation choice: it lets us compare defense learning methods
under the same attack-defense process. Robustness to online adaptive
attackers is outside the main evaluation scope.

The four objectives in the cyber range correspond to the formulation in
Section~\ref{subsec:objectives}: Security Effectiveness, Business
Continuity, Defense Cost, and Critical-Asset Safety. Critical-Asset
Safety is optimized as the fourth objective and is also audited through
semantic replay summaries. The semantic audit is not a fifth optimization
objective; it is used to diagnose whether a policy with strong average
return nevertheless exhibits unacceptable critical-risk behavior.

\begin{table}[t]
\centering
\small
\caption{Main experimental protocol. Detailed hyperparameters and audit rules are reported in the appendix.}
\label{tab:eval-protocol}
\begin{tabular}{p{0.38\linewidth}p{0.50\linewidth}}
\toprule
Item & Setting \\
\midrule
Scenario & CybORG++ Scenario2, fixed baseline attacker process \\
Horizon & \(T=100\) \\
Seeds & \(7, 11, 19\) \\
Objectives & \(4\): Security, Continuity, Cost, Critical-Asset Safety \\
Parallel environments & \(8\) \\
Policy network hidden size & \(128\) \\
Stage~1 archive & \(6\) initial policies, \(98{,}304\) timesteps/seed \\
Stage~2 extension & \(6\) parent policies, \(2\) rounds, \(5\) constrained updates \\
Extension update budget & \(1{,}536\) timesteps/update \\
Return preservation & Non-target preservation multiplier \(\eta\in[1.002,1.008]\); barrier coefficient \(20.0\)\\
Operational limits & Business \(8.0\), cost \(4.0\), critical-dwell slack \(5.0\) \\
Archive evaluation & Preference step \(0.1\), \(3\) episodes per policy--preference pair \\
Assignment evaluation & \(5\) episodes per assigned policy \\
Semantic audit & \(20\) replay episodes per selected policy \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Compared Methods}
\label{subsec:eval-methods}

We compare methods that are evaluated under the same four-objective
protocol and shared assignment thresholds.

\paragraph{DA-CPSL.}
This is the full method. It uses the Stage~1 archive and performs
acceptance-aware Stage~2 extension with Critical-Asset Safety enabled.
The final policy assignment is performed from the acceptable Pareto set.

\paragraph{Stage-1 Archive.}
This ablation evaluates the preference-diverse initialization stage
without Stage~2 extension. It tests whether the initial policy archive is
sufficient to support acceptable assignment.

\paragraph{Weighted-Sum PPO.}
This scalarized baseline~\cite{vanmoffaert2013scalarized,schulman2017ppo} trains independent policies under fixed explicit
preference vectors over the four objectives. It tests whether separately
trained scalarized policies can substitute for an evaluated policy
archive.

\paragraph{Preference-Conditioned PPO.}
This baseline~\cite{yang2019generalized,basaklar2022pdmorl} trains a single policy conditioned on the preference
vector. It tests whether direct preference conditioning can cover the
four-objective trade-off surface without maintaining a separate evaluated
policy archive.

\paragraph{Lagrangian PPO.}
This constrained single-policy baseline~\cite{achiam2017cpo,tessler2018rcpo,liu2020ipo} augments scalarized training with
Lagrangian penalties for operational constraint violations. It tests
whether a single constrained policy can satisfy shared operational limits
under heterogeneous preferences.

\paragraph{Unconstrained Stage~2.}
This ablation uses the same Stage~2 archive-extension structure but
removes acceptance-aware constraint shaping. It tests whether raw Pareto
extension alone is sufficient for operationally acceptable defense.

We do not mix older baselines that were not rerun under the unified
four-objective protocol into the main tables. Such comparisons would
conflate differences in objectives, thresholds, and semantic-audit
protocols.

\subsection{Metrics and Assignment Protocol}
\label{subsec:eval-metrics}

The metrics follow the three-layer structure of
Section~\ref{sec:formulation}: objectives, constraints, and audits.

\paragraph{Objective-space archive metrics.}
To evaluate candidate-set quality, we report hypervolume, expected
utility, coverage ratio, the number of distinct assigned policies, and
the number of retained Pareto records. Hypervolume and expected utility
are standard policy-set MORL metrics~\cite{hayes2022practical,liu2025cmorl,felten2023mogymnasium} for evaluating Pareto-front quality
and preference-conditioned utility; in our setting, they measure
objective-space quality but not operational acceptability.

\paragraph{Operational assignment metrics.}
For each method, we use the same preference grid and shared operational
thresholds. An assignment is acceptable only if the selected policy
satisfies the operational limits defined by the evaluation protocol. We
report feasible-assignment rate, mean constraint violation, assigned
objective returns, final critical compromised hosts, and critical-impact
count. If no acceptable policy exists for a preference, the assignment is
marked infeasible rather than replaced by a high-utility but
unacceptable policy.

\paragraph{Replay-level semantic audit metrics.}
For selected policies, we perform replay-level semantic audit using
\(20\) replay episodes per selected policy. The audit reports
ever-critical-breach rate, persistent-critical-breach rate, critical
dwell, first critical-hit time, near-miss tiers, and phase-conditioned
response behavior. Near-miss statistics are diagnostic: a policy can be
breach-free under the evaluated protocol while still showing
pre-critical exposure. We therefore interpret zero observed critical
breach together with near-miss behavior as evidence of pre-critical
containment, not as evidence of risk elimination.

\paragraph{Assignment protocol.}
For archive-quality evaluation, each candidate policy is evaluated over
the preference grid with the same preference step. For operational
assignment, each method uses the same constrained assignment rule from
Eq.~\ref{eq:constrained-assignment}. For semantic audit, we replay the
selected policy under the fixed evaluation protocol and report
trajectory-level cyber semantics. All reported acceptance results are
empirical under this protocol.

\subsection{Implementation and Reproducibility}
\label{subsec:eval-reproducibility}

All main experiments are run with seeds \(7\), \(11\), and \(19\). The
Stage~2 archive is initialized from the corresponding Stage~1 archive
for the same seed. Candidate-set evaluation, operational assignment, and
semantic audit use the same seed set and the same cyber-range
instantiation.

The implementation stores each candidate as an evaluated record
containing objective estimates, operational violation estimates, and
semantic audit summaries. The artifact package includes the configuration
files for Stage~1, Stage~2, archive-quality evaluation, operational
assignment, table export, and semantic audit. Additional hyperparameters,
semantic rule definitions, per-seed outputs, and full audit vectors are
reported in the appendix.


\section{Evaluation Results}
\label{sec:evaluation-results}

We report the results according to the research questions in
Section~\ref{sec:evaluation-setup}. The evaluation follows the three
layers of the formulation: objective-space archive quality, operational
acceptability, and replay-level cyber semantics. Unless otherwise noted,
all acceptance and semantic results are empirical under the fixed
evaluation protocol.

\subsection{RQ1: Archive Quality}
\label{subsec:results-archive-quality}

DA-CPSL first improves the object it is designed to learn: the
four-objective defense policy archive. Table~\ref{tab:set-quality}
reports objective-space candidate-set quality under the shared
four-objective evaluation protocol.

\begin{table*}[t]
\centering
\small
\caption{Four-objective archive quality. Hypervolume and expected utility measure objective-space policy-set quality; they do not by themselves imply operational acceptability.}
\label{tab:set-quality}
\resizebox{\textwidth}{!}{%
\input{table/set_quality_4obj.tex}
}
\end{table*}

The full DA-CPSL archive achieves the strongest objective-space quality
among the compared methods. It attains the highest hypervolume
(\(2.29\times 10^7\)) and the best expected utility (\(-41.92\)), whereas
Weighted-Sum PPO reaches \(5.84\times 10^6\) hypervolume and \(-161.57\)
expected utility, and Preference-Conditioned PPO reaches
\(3.68\times 10^6\) hypervolume and \(-167.93\) expected utility. This
difference is not only a benchmark-score improvement: it indicates that
the learned archive provides more useful operating points for
organizations with heterogeneous preferences over containment,
continuity, response burden, and Critical-Asset Safety.

The comparison also shows why policy-set quality cannot be inferred from
the number of candidate policies alone. Preference-Conditioned PPO can
produce many distinct assignments, but its archive has substantially
lower hypervolume and expected utility. DA-CPSL instead produces a
smaller but more valuable set of candidate defense policies that better
covers the four-objective trade-off surface.

Figure~\ref{fig:candidate-set-views} visualizes the geometry of the
candidate sets. The projections show that DA-CPSL occupies broader and
more useful regions of the objective space, rather than concentrating
candidate policies in a narrow scalarized region.

\begin{figure*}[t]
\centering
\includegraphics[width=0.48\textwidth]{images/candidate_set_4obj_3d_projection.png}
\includegraphics[width=0.48\textwidth]{images/candidate_set_4obj_pairwise_matrix.png}
\caption{Four-objective candidate-set geometry. The left plot shows a
3D projection, and the right plot shows pairwise objective projections.
The figures illustrate archive coverage rather than operational
acceptability; the latter is evaluated through the acceptance protocol in
Section~\ref{subsec:results-operational-assignment}.}
\label{fig:candidate-set-views}
\end{figure*}

\subsection{RQ2: Operational Assignment Performance}
\label{subsec:results-operational-assignment}

Objective-space quality alone is not sufficient for autonomous defense:
a high-utility policy may still be unacceptable if it violates hard
limits on business disruption, response burden, or critical-risk
behavior. Table~\ref{tab:operational-assignment-performance} evaluates
whether the learned archives support acceptable operational assignment
under the shared limits.

\begin{table*}[t]
\centering
\small
\caption{Operational assignment performance under shared acceptance limits. Feasible rate is the fraction of preferences for which the archive contains an acceptable assigned policy; violation measures the mean magnitude of constraint violation.}
\label{tab:operational-assignment-performance}
\resizebox{\textwidth}{!}{%
\input{table/constrained_deployment_performance_4obj.tex}
}
\end{table*}

DA-CPSL provides the strongest operational-assignment behavior. It
achieves the highest feasible-assignment rate (\(0.6417\)) and the
lowest mean violation (\(0.0942\)), while maintaining zero final
critical compromised hosts and zero observed critical-impact events
under the evaluated protocol. These results support the main operational
claim: DA-CPSL does not merely improve objective-space scores; it
increases the fraction of preferences for which the archive contains an
acceptable policy.

The single-policy baselines fail under the same shared limits.
Weighted-Sum PPO and Lagrangian PPO obtain feasible-assignment rate
\(0.0\), with mean violations \(93.54\) and \(95.61\), respectively.
This suggests that scalarization or single-policy constraint penalties
are insufficient for supporting heterogeneous operational preferences
under hard acceptance constraints.

The comparison between Stage~1 and DA-CPSL shows the role of archive
extension. Stage~2 increases the feasible-assignment rate from \(0.3083\)
to \(0.6417\) and reduces mean violation from \(1.9770\) to \(0.0942\).
Thus, Stage~2 improves not only archive geometry but also the availability
of acceptable assignments.

The comparison with Unconstrained Stage~2 further shows why
acceptance-aware extension is necessary. Unconstrained extension can
improve raw objective-space behavior, but it produces higher mean
violation (\(2.1274\)). The action-level audit in
Section~\ref{subsec:results-semantic-audit} further explains how this
violation is reflected in post-foothold response behavior. This confirms
the distinction introduced in
Section~\ref{subsec:acceptable-pareto-assignment}: raw Pareto quality
does not imply operational acceptability.

\subsection{RQ3: Replay-Level Semantic Audit}
\label{subsec:results-semantic-audit}

The preceding tables evaluate archive quality and assignment
acceptability. We next inspect whether the selected policies exhibit the
intended cyber semantics after an attacker obtains an enterprise
foothold. Table~\ref{tab:rq3-semantic-comparison} reports the
replay-level semantic audit for the selected DA-CPSL policy and the
selected Unconstrained Stage~2 policy.

\begin{table*}[t]
\centering
\small
\caption{Replay-level semantic audit of selected Stage~2 policies. The table reports critical-breach indicators and post-foothold process diagnostics under the fixed replay protocol.}
\label{tab:rq3-semantic-comparison}
\input{table/rq3_semantic_comparison_4obj.tex}
\end{table*}

Both selected Stage~2 policies show no observed critical breach and no
observed persistent critical breach under the evaluated protocol. The
important distinction is therefore not an end-state breach label, but the
post-foothold response process. DA-CPSL reduces post-foothold drift from
\(0.3375\) to \(0.0021\) and removes repeated low-value decoy loops
(\(0.2542\) to \(0.0\)). These differences indicate a more disciplined
response after enterprise foothold: the selected DA-CPSL policy avoids
wasting response capacity on repeated low-value actions and reduces
movement toward critical-risk regions.

Figure~\ref{fig:phase-conditioned-semantics} shows phase-conditioned
response behavior. Compared with Unconstrained Stage~2, DA-CPSL is less
deception-heavy immediately after foothold and places less emphasis on
critical-path-directed actions in the pre-critical phase. Instead, it
uses lower-disruption monitoring, analysis, or non-targeted response
patterns when those actions are sufficient to contain the trajectory
before a realized critical breach.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{images/phase_conditioned_action_semantics.png}
\caption{Phase-conditioned response semantics for selected policies.
The audit compares behavior after enterprise foothold, during
pre-critical exposure, and near critical-risk states.}
\label{fig:phase-conditioned-semantics}
\end{figure}

These findings support a bounded security interpretation. The selected
DA-CPSL policy is breach-free under the evaluated replay protocol, but
near-miss trajectories remain visible. We therefore interpret the result
as evidence of pre-critical containment, not as evidence of risk
elimination.

\subsection{RQ4: Component Necessity}
\label{subsec:results-ablation}

Table~\ref{tab:rq4-ablation-summary} evaluates which components are
responsible for the observed behavior. The ablation separates three
mechanisms: Stage~2 archive extension, acceptance-aware shaping, and
Critical-Asset Safety as a fourth objective.

\begin{table}[t]
\centering
\small
\caption{Ablation summary. The panels isolate the roles of Stage~2 extension, acceptance-aware shaping, and Critical-Asset Safety.}
\label{tab:rq4-ablation-summary}
\input{table/rq4_ablation_summary.tex}
\end{table}

\paragraph{Stage~2 solves an archive-discovery problem.}
Panel A compares Stage~1 with the full Stage~2 method. Stage~2 increases
hypervolume from \(22.17\times 10^6\) to \(22.91\times 10^6\), improves
expected utility from \(-44.88\) to \(-41.92\), increases
feasible-assignment rate from \(0.31\) to \(0.64\), and reduces mean
violation from \(1.98\) to \(0.09\). These results show that Stage~1
provides useful initial policies, but archive extension is needed to
discover more acceptable operating points.

\paragraph{Acceptance-aware shaping solves an acceptability problem.}
Panel B compares DA-CPSL with the Unconstrained Stage~2 ablation. Removing
acceptance-aware shaping reduces feasible-assignment rate and increases
mean violation. It also increases questionable post-foothold behavior in
the semantic audit. This shows that Pareto extension alone is not enough:
extension must be constrained by operational and critical-risk
acceptance criteria.

\paragraph{Critical-Asset Safety solves a representation problem.}
Panel C compares the four-objective formulation with a three-objective
variant that lacks Critical-Asset Safety. The three-objective variant can
appear competitive in conventional objective-space terms, but it fails
under the critical-risk semantics required by the current threat model:
its selected policies exhibit high ever-critical-breach and persistent
critical-breach rates, whereas the four-objective formulation keeps these
indicators near zero under the evaluated protocol. This demonstrates that
Critical-Asset Safety is not a cosmetic auxiliary term; it changes the
representation of acceptable defense trade-offs.

\subsection{Summary of Findings}
\label{subsec:results-summary}

The evaluation supports four findings. First, DA-CPSL constructs a
stronger four-objective defense policy archive than scalarized and
non-acceptance-aware alternatives. Second, the archive supports more
acceptable operational assignments under shared hard limits. Third,
semantic replay audit shows that the selected DA-CPSL policy changes the
post-foothold response process, reducing drift and low-value action loops
while keeping observed critical breach at zero under the protocol.
Fourth, the ablation study shows that Stage~2 extension, acceptance-aware
shaping, and Critical-Asset Safety each address a distinct failure mode:
discovery, acceptability, and representation.

Taken together, the results support the following bounded claim:
DA-CPSL provides stronger pre-critical containment under the evaluated
threat model and cyber-range protocol. They do not imply formal safety,
readiness for unsupervised production operation, or elimination of all
cyber risk.


\section{Related Work}
\label{sec:related-work}

\subsection{Autonomous Cyber Defense and Cyber Ranges}

Autonomous cyber defense has emerged as a research direction for using
learning agents to detect, contain, and respond to attacker behavior in
networked environments. Surveys of automated and reinforcement-learning
based cyber defense emphasize that the field requires both realistic
cyber environments and decision-making agents capable of operating under
partial observability, large action spaces, and adversarial dynamics
~\cite{vyas2023automated,palmer2023drlsurvey,thompson2024entity,palmer2025egta}. CybORG introduced a gym
for autonomous cyber agents, and subsequent work has used CybORG and
CAGE-style challenge settings to study learned network defenders
~\cite{standen2021cyborg,foley2022autonomous}. CybORG++ extends this
line with improved tooling and faster experimentation for enterprise
network defense research~\cite{emerson2024cyborgpp}.

Most cyber-range ACD work evaluates a trained defender by episodic
reward, action success, or scenario-specific measures. Foundational
studies have examined tabular learning and deep RL defenders in cyber
ranges~\cite{applebaum2022bridging,foley2022autonomous}, while reward
shaping work has shown that the training signal strongly affects learned
defense behavior~\cite{bates2023reward}. Our work uses the same general
cyber-range tradition but addresses a different decision problem. Rather
than training one defense policy for a single scalar reward, DA-CPSL
learns an evaluated archive of defense policies and performs constrained
assignment under operational preferences and hard limits.

\subsection{Defensive Trade-offs and Operational Constraints}

Cyber defense actions often have operational side effects. Restoring a
host may remove attacker presence but disrupt services; deception may
slow attacker progression but consume response resources; conservative
response may preserve short-term continuity while increasing exposure
around critical assets. This motivates treating autonomous defense as a
decision problem with competing objectives rather than as pure attack
suppression~\cite{odriscoll2024morlcyber}. In critical-service or IT/OT-adjacent environments, this
trade-off is particularly important because OT security guidance
emphasizes safety, reliability, and continuity alongside cybersecurity
risk reduction~\cite{nist_sp80082r3}.

Existing ACD and cyber-range studies recognize operationally meaningful
metrics, but they usually do not formalize the defender's decision object
as a policy archive with hard acceptance constraints. Our formulation
separates preferences from hard limits: preferences rank acceptable
trade-offs, while operational constraints reject unacceptable behavior
regardless of scalar utility. This distinction is central to DA-CPSL and
to our evaluation, where objective-space quality, operational
assignment, and replay-level semantic audit are reported separately.

\subsection{Safe, Constrained, and Assured Learning}

Constrained reinforcement learning studies policies that maximize return
while satisfying expected cost or safety constraints. Constrained MDPs
provide the classical formulation~\cite{altman2021cmdp,dutta2021constraints}, and policy
optimization methods such as CPO and interior-point policy optimization
provide algorithmic mechanisms for constraint-aware learning
~\cite{schulman2015trpo,achiam2017cpo,liu2020ipo,tessler2018rcpo,xu2021crpo,paternain2019duality}. These methods are closely related to
our Stage~2 extension problem, where a policy improves one objective
while preserving other objective values and satisfying operational
limits.

Our setting differs from conventional constrained RL in two ways. First,
we do not train a single policy for one constrained objective. DA-CPSL
constructs a policy archive so that different operator preferences can be
handled by assignment rather than retraining. Second, our constraints
include replay-level critical-risk semantics and empirical operational
acceptance. We therefore do not claim formal safety guarantees. Instead,
we treat acceptance as an empirical filter under the stated evaluation
protocol and report semantic audit evidence to expose critical-risk
behavior that may be hidden by average return.

\subsection{Multi-Objective Reinforcement Learning and Policy-Set Methods}

Multi-objective reinforcement learning addresses settings where multiple
objectives must be optimized jointly and decision makers may have
different preferences over those objectives~\cite{roijers2013survey,hayes2022practical,parisi2016pareto,lu2023morl}.
One common approach trains a single preference-conditioned policy that
takes the preference vector as input~\cite{yang2019generalized,basaklar2022pdmorl}. Another
approach trains or maintains a finite set of policies to approximate a
Pareto set, enabling later policy assignment for a given preference
~\cite{vanmoffaert2014sets,xu2020prediction,alegre2022optimistic,alegre2023gpi,felten2024morld,ropke2024divide,zahavy2021policies}.
Policy-set methods are attractive for autonomous defense because they
allow the defender to adapt to changing operational preferences without
retraining a new policy for every preference.

C-MORL is particularly relevant because it uses a two-stage policy-set
learning procedure: preference-diverse initialization followed by
Pareto-front extension, where one objective is improved while the others
are constrained~\cite{liu2025cmorl,huang2022constrainedmorl,laumanns2006epsilon}. It then performs policy assignment
from the learned Pareto set. DA-CPSL adopts this policy-set learning
philosophy, but changes the problem object and assignment rule for
autonomous defense. Each candidate policy is evaluated by objective
returns, operational violation summaries, and semantic audit variables;
final assignment is performed from the acceptable Pareto set; and the
assignment may return infeasible when no candidate satisfies the hard
limits. Thus, DA-CPSL is not only a MORL archive-construction method
applied to a cyber range. It is a defense-aware, acceptance-aware, and
audit-aware policy-set learning framework for autonomous cyber defense
under operational constraints.

\section{Conclusion}
\label{sec:conclusion}

This paper studied autonomous cyber defense as a constrained
multi-objective policy-archive problem rather than a single
scalar-reward policy-learning problem. We argued that operationally
meaningful defense requires separating three concerns that are often
collapsed in reinforcement-learning formulations: objective trade-offs,
hard acceptance constraints, and preference-conditioned policy
assignment. In particular, we introduced Critical-Asset Safety as a
first-class objective to capture tail-risk behavior around critical
assets, while keeping replay-level semantic audit as an explicit
diagnostic mechanism.

We proposed DA-CPSL, a defense-aware constrained policy-set learning
method that constructs an evaluated archive of candidate defense
policies. DA-CPSL combines preference-diverse initialization,
acceptance-aware Pareto extension, semantic candidate auditing, and
constrained assignment over the acceptable Pareto set. This design allows
operator preferences to select among acceptable policies while preventing
preference weights from overriding hard operational limits. If no
candidate satisfies the specified limits, DA-CPSL reports the assignment
as infeasible rather than forcing an unacceptable policy choice.

Our controlled cyber-range evaluation shows that DA-CPSL improves both
objective-space archive quality and operational assignment
acceptability. Across the evaluated seeds, DA-CPSL achieves stronger
hypervolume and expected utility than scalarized and non-acceptance-aware
alternatives, increases the feasible-assignment rate, and substantially
reduces mean constraint violation. Replay-level semantic audit further
shows no observed critical breach or persistent critical breach for the
selected DA-CPSL policy under the fixed evaluation protocol, while
near-miss trajectories remain visible. This supports a bounded
interpretation: DA-CPSL improves pre-critical containment under the
evaluated threat model and cyber-range protocol, but it does not
eliminate cyber risk.

The results also clarify the role of the main components. Stage~2
extension improves archive discovery; acceptance-aware shaping improves
operational acceptability; and Critical-Asset Safety changes the
representation of acceptable defense trade-offs. These findings suggest
that autonomous defense should be evaluated not only by average return or
raw Pareto quality, but also by whether selected policies satisfy
operational limits and exhibit acceptable replay-level security
semantics.

Several limitations remain. Our evaluation uses a controlled cyber-range
instantiation with a fixed attacker process for fair comparison across
methods. The reported acceptance and breach-free results are empirical
under this protocol and should not be interpreted as formal safety
guarantees, readiness for unsupervised production operation, or
robustness to all attacker variants. Future work should extend
DA-CPSL-style policy archives to broader attacker variation, richer
incident-response workflows, human approval and escalation processes,
and complementary formal or runtime assurance mechanisms for learned
defense policies.


% =========================
% Bibliography
% =========================
\bibliographystyle{ACM-Reference-Format}
\bibliography{refs}

% =========================
% Appendices required / useful for CCS 2026
% =========================
\appendix


\appendix

\section{Additional Formal Details}
\label{app:formal-details}

This appendix provides formal details that support the main formulation
and method. The main paper keeps only the definitions required for the
core argument; this appendix gives the complete audit variables,
empirical acceptance estimates, DA-CPSL workflow, extension surrogate,
and the relationship to C-MORL.

\subsection{Critical-Risk Audit Variables}
\label{app:critical-audit}

Let \(\mathcal{E}\) denote the set of cyber entities and
\(\mathcal{K}\subseteq\mathcal{E}\) denote the critical-asset set. For
an audit trajectory, let \(C_t\subseteq\mathcal{E}\) be the set of
compromised entities at time \(t\). As in the main text, we define the
critical-breach indicator as:
\begin{equation}
b_t^{\mathrm{crit}}
=
\mathbb{1}\{C_t\cap\mathcal{K}\neq\emptyset\}.
\label{eq:app-critical-breach}
\end{equation}
We define the enterprise-foothold indicator as:
\begin{equation}
b_t^{\mathrm{foot}}
=
\mathbb{1}\{C_t\setminus\mathcal{K}\neq\emptyset\}.
\label{eq:app-enterprise-foothold}
\end{equation}
We define the pre-critical exposure indicator as:
\begin{equation}
b_t^{\mathrm{pre}}
=
\mathbb{1}
\left\{
C_t\cap\mathcal{K}=\emptyset
\;\wedge\;
\rho(C_t,\mathcal{K})>0
\right\},
\label{eq:app-precritical}
\end{equation}
where \(\rho(C_t,\mathcal{K})\) is a policy-independent audit function
that measures elevated exposure around critical assets.

The first critical-hit indicator is defined as:
\begin{equation}
f_t^{\mathrm{crit}}
=
\mathbb{1}
\left\{
b_t^{\mathrm{crit}}=1
\;\wedge\;
\sum_{k=0}^{t-1}
b_k^{\mathrm{crit}}=0
\right\}.
\label{eq:app-first-hit-indicator}
\end{equation}
We use the convention that an empty sum is zero, so this definition is
valid at \(t=0\). For a persistence horizon \(L\), the step-level
persistent critical compromise indicator is defined as:
\begin{equation}
p_{t,L}^{\mathrm{crit}}
=
\mathbb{1}
\left\{
t\geq L-1
\;\wedge\;
\sum_{k=t-L+1}^{t}
b_k^{\mathrm{crit}}
=
L
\right\}.
\label{eq:app-persistent-step}
\end{equation}
The special case \(L=2\) corresponds to critical compromise persisting
for two consecutive timesteps.

For a trajectory \(\tau\), we define the event-level critical breach
indicator as:
\begin{equation}
B_{\mathrm{crit}}(\tau)
=
\mathbb{1}
\left\{
\sum_{t=0}^{T-1}
b_t^{\mathrm{crit}}>0
\right\}.
\label{eq:app-ever-critical}
\end{equation}
We define critical dwell as:
\begin{equation}
D_{\mathrm{crit}}(\tau)
=
\sum_{t=0}^{T-1}
b_t^{\mathrm{crit}}.
\label{eq:app-critical-dwell}
\end{equation}
We define the first critical-hit time as:
\begin{equation}
T_{\mathrm{crit}}(\tau)
=
\min\{t:b_t^{\mathrm{crit}}=1\},
\qquad
T_{\mathrm{crit}}(\tau)=\infty
\text{ if no critical breach occurs}.
\label{eq:app-first-critical-time}
\end{equation}
When a finite mean first critical-hit time is reported, it is computed
over trajectories with \(B_{\mathrm{crit}}(\tau)=1\); no-breach
trajectories are reported separately through \(B_{\mathrm{crit}}\).
We define the trajectory-level persistent critical breach indicator as:
\begin{equation}
B_{\mathrm{persist}}^{(L)}(\tau)
=
\mathbb{1}
\left\{
\sum_{t=0}^{T-1}
p_{t,L}^{\mathrm{crit}}>0
\right\}.
\label{eq:app-persistent-trajectory}
\end{equation}
The step-level persistence indicator may be active for multiple
overlapping windows; the trajectory-level event records whether at least
one such window occurs.

These trajectory-level variables are used for replay-level semantic
audit, event-style operational constraints, and diagnostic reporting. They
are not privileged inputs to the defense policy.

\subsection{Empirical Operational Acceptance}
\label{app:empirical-acceptance}

The population-level operational violation summary is defined in
Eq.~\ref{eq:constraint-expectation}. In experiments, it is estimated from
a fixed rollout protocol. For \(N_{\mathrm{eval}}\) evaluation
trajectories \(\tau_1,\ldots,\tau_{N_{\mathrm{eval}}}\), we estimate
\(g_m(\pi)\) as:
\begin{equation}
\widehat{g}_m(\pi)
=
\frac{1}{N_{\mathrm{eval}}}
\sum_{j=1}^{N_{\mathrm{eval}}}
c_m(\tau_j),
\qquad
\tau_j\sim\mathbb{P}_{\mathcal{M}}^\pi .
\label{eq:app-empirical-g}
\end{equation}
The corresponding empirical mean violation is defined as:
\begin{equation}
\widehat{V}(\pi;\boldsymbol{\delta})
=
\frac{1}{M}
\sum_{m=1}^{M}
\left[
\widehat{g}_m(\pi)-\delta_m
\right]_+ .
\label{eq:app-empirical-violation}
\end{equation}

Assuming independent evaluation trajectories and \(c_m(\tau)\in[0,1]\),
a standard Hoeffding bound gives the following one-sided confidence
statement. For any \(\alpha\in(0,1)\), with probability at least
\(1-\alpha\):
\begin{equation}
g_m(\pi)
\leq
\widehat{g}_m(\pi)
+
\sqrt{
\frac{\log(1/\alpha)}{2N_{\mathrm{eval}}}
}.
\label{eq:app-hoeffding}
\end{equation}
For event-style constraints with zero observed events over
\(N_{\mathrm{event}}\) independent event-level replay runs, the exact
one-sided Clopper--Pearson upper bound for an event probability \(p_m\)
is:
\begin{equation}
p_m
\leq
1-\alpha^{1/N_{\mathrm{event}}}
\qquad
\text{with confidence }1-\alpha .
\label{eq:app-zero-event-bound}
\end{equation}
Thus, observing no critical breach under a finite evaluation protocol is
evidence of low event rate under that protocol, but it is not a formal
guarantee of zero risk across all executions or attacker variants.

\subsection{Candidate Records and Archive Views}
\label{app:candidate-records}

DA-CPSL stores each evaluated candidate as:
\begin{equation}
x(\pi)
=
\left(
\pi,
\widehat{\mathbf{J}}(\pi),
\widehat{\mathbf{g}}(\pi),
\widehat{\boldsymbol{\psi}}(\pi)
\right).
\label{eq:app-candidate-record}
\end{equation}
Here, \(\widehat{\mathbf{J}}(\pi)\) estimates the optimized objective
returns, \(\widehat{\mathbf{g}}(\pi)\) estimates operational violation
summaries, and \(\widehat{\boldsymbol{\psi}}(\pi)\) records semantic
audit variables such as \(B_{\mathrm{crit}}(\tau)\),
\(B_{\mathrm{persist}}^{(L)}(\tau)\), \(T_{\mathrm{crit}}(\tau)\), and
\(D_{\mathrm{crit}}(\tau)\).

We use \(\Pi\) to denote the policy identities in the archive; each
\(\pi\in\Pi\) is associated with an evaluated record \(x(\pi)\).
The raw archive is the set of all evaluated candidates. The acceptable
archive for operational limits \(\boldsymbol{\delta}\) is defined as in
Eq.~\ref{eq:acceptable-archive}. The raw Pareto set and acceptable
Pareto set are different views of the same evaluated archive. This
distinction matters because a raw dominated policy may remain useful when
the policies dominating it fail operational acceptance constraints.

\section{DA-CPSL Algorithmic Details}
\label{app:dacpsl-details}

\subsection{Parent Selection}
\label{app:parent-selection}

At each extension round, DA-CPSL selects parent policies from an archive
view. If the acceptable Pareto set is non-empty, the parent source set is
defined as:
\begin{equation}
\Pi_{\mathrm{src}}
=
\Pi_{\mathrm{P}}^{\mathrm{acc}}(\Pi,\boldsymbol{\delta}).
\label{eq:app-parent-source-acceptable}
\end{equation}
If no acceptable Pareto candidate exists early in training, the parent
source set may fall back to the raw Pareto set:
\begin{equation}
\Pi_{\mathrm{src}}
=
\Pi_{\mathrm{P}}(\Pi).
\label{eq:app-parent-source-raw}
\end{equation}
Candidates selected from the raw Pareto set remain ineligible for
operational assignment unless they later pass the acceptance filter.

To encourage objective-space coverage, we compute crowd-distance-style
diversity scores over calibrated objective vectors. Let
\(\widetilde{J}_i(\pi)\) be the calibrated value of objective \(i\).
For each objective \(i\), sort candidates in \(\Pi_{\mathrm{src}}\) by
\(\widetilde{J}_i\). Extreme candidates for each objective are retained
separately, equivalently assigned infinite crowd distance before
truncation. For an interior candidate \(\pi\), let \(\pi_i^{-}\) and
\(\pi_i^{+}\) denote its immediate neighbors in the sorted order for
objective \(i\). We define the diversity score as:
\begin{equation}
D_{\mathrm{crowd}}(\pi)
=
\sum_i
\frac{
\widetilde{J}_i(\pi_i^{+})
-
\widetilde{J}_i(\pi_i^{-})
}{
\max_{\pi'\in\Pi_{\mathrm{src}}}\widetilde{J}_i(\pi')
-
\min_{\pi'\in\Pi_{\mathrm{src}}}\widetilde{J}_i(\pi')
+
\epsilon
}.
\label{eq:app-crowd-distance}
\end{equation}
Among the remaining interior candidates, DA-CPSL prioritizes high
diversity, low mean constraint violation, and lower semantic
critical-risk summaries.

\subsection{Extension Surrogate}
\label{app:extension-surrogate}

The main text defines the idealized extension problem in
Eq.~\ref{eq:ideal-extension}. In a PPO-style implementation, we optimize
a sampled surrogate that combines target-objective improvement,
return-preservation terms, and violation penalties. Let \(\pi_\theta\)
denote the child policy parameterized by \(\theta\), and define the
non-target preservation slack as:
\begin{equation}
s_i(\theta)
=
\widehat{\widetilde{J}}_{i}(\pi_\theta)
-
d_i(\pi_r;\eta_i),
\qquad i\in\mathcal{I},\; i\neq \ell .
\label{eq:app-preservation-slack}
\end{equation}
For sampled updates that remain inside the barrier domain
\(s_i(\theta)>0\) for all \(i\in\mathcal{I}\), \(i\neq\ell\), we write
the extension surrogate as:
\begin{align}
\mathcal{L}_{\mathrm{ext}}(\theta;\pi_r,\ell)
=
&\;
\mathcal{L}^{\mathrm{clip}}_{\ell}(\theta)
+
\frac{1}{t_J}
\sum_{i\in\mathcal{I},\, i\neq \ell}
\log
\left(
s_i(\theta)
+
\epsilon
\right)
\label{eq:app-extension-surrogate}
\\
&\;
-
\lambda_g
\sum_{m=1}^{M}
\left[
\widehat{q}_m(\pi_\theta)
-
\delta_m
\right]_+
-
\lambda_{\psi}
\widehat{\Psi}_{\mathrm{crit}}(\pi_\theta).
\nonumber
\end{align}
Here, \(\mathcal{L}^{\mathrm{clip}}_{\ell}(\theta)\) is a PPO-style
clipped surrogate for the target objective \(\ell\), \(t_J>0\) is the
return-preservation barrier parameter, \(\epsilon>0\) is a numerical
stabilizer, \(\widehat{q}_m\) denotes a training proxy for operational
violations, and \(\widehat{\Psi}_{\mathrm{crit}}\) denotes a
critical-risk penalty derived from semantic diagnostics. If a sampled
update leaves the barrier domain, the resulting child is not accepted by
the barrier surrogate and is evaluated through the post-update acceptance
protocol.

The surrogate in Eq.~\ref{eq:app-extension-surrogate} is a training
mechanism. It is not the final acceptance decision. After the update,
each child policy is re-evaluated under the fixed evaluation protocol to
obtain \(\widehat{\mathbf{J}}\), \(\widehat{\mathbf{g}}\), and
\(\widehat{\boldsymbol{\psi}}\). Operational eligibility is determined
from these post-update estimates.

\subsection{Full Algorithm}
\label{app:full-algorithm}

\noindent
\textbf{Algorithm A1: DA-CPSL}

\noindent
\textbf{Input:}
initial preference set \(\mathcal{W}_{\mathrm{init}}\);
objective index set \(\mathcal{I}\);
operational limits \(\boldsymbol{\delta}\);
parent budget \(N_{\mathrm{parent}}\);
extension rounds \(K_{\mathrm{ext}}\);
return-preservation multiplier \(\eta\);
evaluation protocol \(\mathcal{D}_{\mathrm{eval}}\).

\noindent
\textbf{Output:}
evaluated policy archive \(\Pi\) and constrained assignment rule.

\begin{enumerate}
\item Initialize the evaluated archive as \(\Pi\leftarrow\emptyset\).

\item \textbf{Stage 1: preference-diverse initialization.}
For each \(\mathbf{w}\in\mathcal{W}_{\mathrm{init}}\):
\begin{enumerate}
\item Train a defense policy \(\pi_{\mathbf{w}}\) using the scalarized
return in Eq.~\ref{eq:stage1-scalarized-return}.
\item Evaluate \(\pi_{\mathbf{w}}\) under
\(\mathcal{D}_{\mathrm{eval}}\) to obtain
\[
x(\pi_{\mathbf{w}})
=
\left(
\pi_{\mathbf{w}},
\widehat{\mathbf{J}}(\pi_{\mathbf{w}}),
\widehat{\mathbf{g}}(\pi_{\mathbf{w}}),
\widehat{\boldsymbol{\psi}}(\pi_{\mathbf{w}})
\right).
\]
\item Add \(x(\pi_{\mathbf{w}})\) to \(\Pi\).
\end{enumerate}

\item \textbf{Stage 2: acceptance-aware Pareto extension.}
For extension round \(k=1,\ldots,K_{\mathrm{ext}}\):
\begin{enumerate}
\item Compute the raw Pareto set \(\Pi_{\mathrm{P}}(\Pi)\) and the
acceptable Pareto set
\(\Pi_{\mathrm{P}}^{\mathrm{acc}}(\Pi,\boldsymbol{\delta})\).
\item Select up to \(N_{\mathrm{parent}}\) parent policies using the
parent-selection rule in Appendix~\ref{app:parent-selection}.
\item For each selected parent \(\pi_r\) and each target objective
\(\ell\in\mathcal{I}\):
\begin{enumerate}
\item Train a child policy by optimizing the idealized problem in
Eq.~\ref{eq:ideal-extension}, or its PPO-style surrogate in
Eq.~\ref{eq:app-extension-surrogate}.
\item Evaluate the child under \(\mathcal{D}_{\mathrm{eval}}\) to obtain
\(x(\pi_{\ell}^{+})\).
\item Add the evaluated child record to \(\Pi\).
\end{enumerate}
\end{enumerate}

\item \textbf{Operational assignment.}
For a given preference and limit pair
\((\mathbf{w},\boldsymbol{\delta})\), compute
\(\Pi_{\mathrm{P}}^{\mathrm{acc}}(\Pi,\boldsymbol{\delta})\). If the set
is empty, return \emph{infeasible}; otherwise return
\[
\pi^\star(\mathbf{w},\boldsymbol{\delta})
=
\arg\max_{\pi\in
\Pi_{\mathrm{P}}^{\mathrm{acc}}(\Pi,\boldsymbol{\delta})}
\mathbf{w}^{\top}
\widetilde{\mathbf{J}}(\pi).
\]
\end{enumerate}

\section{Relationship to C-MORL}
\label{app:cmorl-relation}

DA-CPSL uses the two-stage policy-set learning structure as an archive
construction template. The main text focuses on the autonomous-defense
method. This appendix summarizes the relationship to C-MORL at the
method level.

\begin{table*}[t]
\centering
\caption{Method-level relationship between C-MORL and DA-CPSL.}
\label{tab:cmorl-relation}
\small
\begin{tabular}{p{0.19\linewidth}p{0.36\linewidth}p{0.36\linewidth}}
\toprule
Component & C-MORL & DA-CPSL \\
\midrule
Problem setting
& Generic multi-objective reinforcement-learning benchmarks.
& Autonomous defense under partial observability, operational side
effects, and critical-asset risk. \\

Objective vector
& Benchmark-defined objective returns.
& Security Effectiveness, Business Continuity, Defense Cost, and
Critical-Asset Safety. \\

Candidate record
& Policy and expected return vector.
& Policy, objective estimates, operational violation summaries, and
semantic audit summaries. \\

Parent selection
& Pareto candidates selected using objective-space diversity, such as
crowd distance.
& Parent selection uses objective-space diversity while prioritizing
operationally acceptable and lower-critical-risk candidates. \\

Extension step
& Improve one objective while constraining other objective returns.
& Improve one objective while preserving other objectives and respecting
operational and critical-risk acceptance constraints. \\

Assignment
& Set Max Policy selection from a Pareto set for a given preference.
& Constrained assignment from an acceptable Pareto set; the assignment
may return infeasible. \\

Evaluation implication
& Hypervolume, expected utility, and sparsity characterize the policy
set.
& Candidate-set quality is evaluated together with feasible-assignment
rate, mean violation, and replay-level critical-risk audit. \\
\bottomrule
\end{tabular}
\end{table*}

We do not claim that DA-CPSL inherits all theoretical guarantees of
C-MORL unchanged. DA-CPSL adds empirical operational constraints,
trajectory-level semantic audits, and finite-rollout acceptance
decisions. The C-MORL extension principle provides the policy-set
learning backbone; the defense-specific contribution is the
acceptance-aware and audit-aware adaptation required by autonomous
defense.

\section{Theoretical Properties of the Idealized Formulation}
\label{app:theoretical-properties}

This appendix states basic properties of the idealized formulation. These
properties concern the mathematical objects defined in the paper; they
are not formal guarantees of production-network safety~\cite{rockafellar1970convex,borkar2009stochastic}.

\paragraph{Proposition D.1: Monotone calibration preserves dominance.}
Suppose \(\mathcal{C}\) is coordinate-wise monotone. If
\(\pi_a\succ_J \pi_b\), then
\(\mathcal{C}(\mathbf{J}(\pi_a))\) weakly dominates
\(\mathcal{C}(\mathbf{J}(\pi_b))\), with strict improvement in at least
one objective whenever \(\mathcal{C}\) is strictly monotone on that
coordinate.

\emph{Proof sketch.}
Pareto dominance requires \(J_i(\pi_a)\geq J_i(\pi_b)\) for every
objective \(i\), with strict inequality for at least one objective.
Coordinate-wise monotonicity of \(\mathcal{C}\) preserves the weak
inequality in every coordinate. If \(\mathcal{C}\) is strictly monotone
on a coordinate where the original inequality is strict, the calibrated
vector also has a strict improvement on that coordinate.

\paragraph{Proposition D.2: Assignment optimality within the acceptable Pareto set.}
If
\(\Pi_{\mathrm{P}}^{\mathrm{acc}}(\Pi,\boldsymbol{\delta})\neq\emptyset\),
then the assignment rule in Eq.~\ref{eq:constrained-assignment} returns a
policy that maximizes calibrated linear utility over the acceptable
Pareto set.

\emph{Proof sketch.}
The result follows directly from the definition of
\(\pi^\star(\mathbf{w},\boldsymbol{\delta})\) as the maximizer of
\(\mathbf{w}^{\top}\widetilde{\mathbf{J}}(\pi)\) over
\(\Pi_{\mathrm{P}}^{\mathrm{acc}}(\Pi,\boldsymbol{\delta})\). If the
set is empty, the method returns infeasible by definition.

\paragraph{Proposition D.3: Acceptance filtering enforces the specified constraints.}
For any policy \(\pi\in\Pi_{\mathrm{acc}}(\Pi,\boldsymbol{\delta})\),
we have \(g_m(\pi)\leq\delta_m\) for all \(m=1,\ldots,M\).

\emph{Proof sketch.}
This follows immediately from the definition of
\(\Pi_{\mathrm{acc}}(\Pi,\boldsymbol{\delta})\) in
Eq.~\ref{eq:acceptable-archive}. The empirical version enforces the
empirical acceptance rule with \(\widehat{g}_m\) replacing \(g_m\) under
the evaluation protocol; it does not imply population-level satisfaction
without additional statistical assumptions.


\section{Additional Experimental Details}
\label{app:experimental-details}

This appendix provides experimental details that support
Sections~\ref{sec:evaluation-setup} and~\ref{sec:evaluation-results}.
The main text reports the core evaluation evidence; this appendix
expands the evaluation protocol, semantic-audit definitions,
replay-level audit vectors, objective-level ablations, and an
attacker-shift robustness probe.

\subsection{Objective Signal Mapping}
\label{app:objective-signal-mapping}

The four objectives in the main text are instantiated through
cyber-defense signals collected during rollouts. Security Effectiveness
captures attacker suppression, recovery from compromise, and reduction of
security exposure. Business Continuity captures service preservation and
disruption induced by defensive interventions. Defense Cost captures
response burden, including reliance on costly or high-disruption actions.
Critical-Asset Safety captures critical-risk behavior, including
critical breach, persistent critical compromise, first critical-hit time,
and critical dwell.

\begin{table*}[t]
\centering
\small
\caption{Objective-to-signal mapping used in the evaluation. The table
summarizes semantic roles rather than implementation-specific
coefficients.}
\label{tab:app-objective-signal-map}
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.08}
\resizebox{\textwidth}{!}{%
\begin{tabular}{p{0.20\linewidth}p{0.32\linewidth}p{0.18\linewidth}p{0.20\linewidth}}
\toprule
Objective & Representative Signals & Optimization Direction & Evaluation Role \\
\midrule
Security Effectiveness
& Attacker suppression, compromise reduction, recovery from compromised enterprise hosts
& Maximize
& Objective-space return and assignment utility \\

Business Continuity
& Service preservation, disruption induced by defensive intervention
& Maximize
& Objective-space return and operational acceptance \\

Defense Cost
& Response burden, high-disruption action usage, costly intervention patterns
& Maximize after utility transformation
& Objective-space return and operational acceptance \\

Critical-Asset Safety
& Critical breach, persistent critical compromise, first critical-hit time, critical dwell
& Maximize
& Objective-space return, acceptance filtering, and semantic audit \\
\bottomrule
\end{tabular}
}
\end{table*}

The mapping separates optimized objectives from replay-level audit
variables. Critical-Asset Safety is optimized as a first-class objective,
while semantic audit variables diagnose whether selected policies exhibit
unacceptable tail-risk behavior around critical assets.

\subsection{Evaluation Protocol Details}
\label{app:evaluation-protocol}

All methods are evaluated under the same four-objective protocol, the
same preference grid, the same shared operational limits, and the same
constrained assignment rule. Policy-set methods are evaluated at both
the archive level and the operational-assignment level.

Selected policies are additionally replayed through the semantic audit.
The audit partitions trajectories into enterprise-foothold,
pre-critical, and critical-risk phases before computing risk-tier
statistics, questionable-response rules, and pre-critical action-family
summaries. This separation is important because two policies can share
the same final breach label while exhibiting materially different
post-foothold response behavior.

\begin{table*}[t]
\centering
\small
\caption{Summary of the appendix evaluation protocol. Main-text results
use the same protocol; the appendix expands the metrics and case-level
diagnostics.}
\label{tab:app-eval-protocol}
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.08}
\resizebox{\textwidth}{!}{%
\begin{tabular}{p{0.22\linewidth}p{0.28\linewidth}p{0.38\linewidth}}
\toprule
Evaluation Layer & Reported Evidence & Purpose \\
\midrule
Archive quality
& Hypervolume, expected utility, coverage, assigned-policy diversity
& Measures whether the learned archive covers useful four-objective trade-offs \\

Operational assignment
& Feasible-assignment rate, mean violation, final critical hosts, critical-impact count
& Measures whether preferences can be assigned to policies that pass shared operational limits \\

Replay-level semantic audit
& Critical breach, persistent breach, critical dwell, first critical-hit time, phase-conditioned behavior
& Diagnoses whether selected policies contain attacker progression before critical breach \\

Ablation and robustness
& Objective-level ablation, attacker-shift probe
& Tests component necessity and exposes boundary conditions of the evaluated method family \\
\bottomrule
\end{tabular}
}
\end{table*}

\subsection{Semantic Audit Rule Families}
\label{app:semantic-rules}

The semantic audit reports two forms of evidence. The first is
hard critical-risk evidence, including whether a replay ever reaches
critical breach, whether critical compromise persists, and how many steps
critical compromise lasts. The second is process-level evidence,
including questionable response behavior after enterprise foothold and
action-family patterns in the pre-critical window.

We use the following audit terms. \emph{Ever Critical Breach} denotes
whether a critical asset is compromised at any point in the replay.
\emph{Persistent Critical Breach} denotes whether critical compromise
persists across the persistence window. \emph{Critical Dwell} denotes
the number of replay steps in which critical compromise is present.
\emph{Near-Miss} denotes trajectories that approach critical exposure but
do not realize critical breach under the evaluated protocol.
Questionable-response rules diagnose post-foothold drift, delayed
critical response, repeated low-value deception loops, and other
trajectory patterns that may indicate operationally poor response
discipline.

\begin{table*}[t]
\centering
\small
\caption{Semantic-audit rule families. The audit variables are diagnostic
signals and do not constitute formal safety guarantees.}
\label{tab:app-semantic-rule-families}
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.08}
\resizebox{\textwidth}{!}{%
\begin{tabular}{p{0.24\linewidth}p{0.38\linewidth}p{0.26\linewidth}}
\toprule
Rule Family & Meaning & Role in the Paper \\
\midrule
Critical-breach indicators
& Whether any critical asset is compromised during replay
& Hard semantic evidence for critical-asset failure \\

Persistence and dwell
& Whether critical compromise persists and how long it remains present
& Distinguishes transient exposure from sustained critical compromise \\

Risk tiers
& Coarse trajectory categories ranging from no critical exposure to persistent critical compromise
& Summarizes replay-level risk severity \\

Post-foothold response rules
& Process-level indicators of drift, delayed response, or repeated low-value response patterns
& Diagnoses response discipline after enterprise foothold \\

Pre-critical action summaries
& Action-family and target-focus statistics before realized critical breach
& Explains how a policy contains or approaches critical-risk states \\
\bottomrule
\end{tabular}
}
\end{table*}

\section{Additional Replay-Level Semantic Audit}
\label{app:semantic-audit}

\subsection{Full Semantic Audit Vector}
\label{app:rq3-full-audit}

Table~\ref{tab:rq3-semantic-full-audit} expands the replay-level
semantic audit reported in the main text. The selected DA-CPSL and
Unconstrained Stage~2 policies both avoid observed critical breach under
the fixed replay protocol, but their process-level audit variables differ
substantially.

\begin{table*}[t]
\centering
\small
\caption{Full replay-level semantic audit vector for selected Stage~2
policies. Lower values are better for critical-breach indicators,
critical dwell, and questionable-response rules.}
\label{tab:rq3-semantic-full-audit}
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.08}
\resizebox{\textwidth}{!}{%
\begin{tabular}{p{0.26\linewidth}ccp{0.38\linewidth}}
\toprule
Metric & DA-CPSL & Unconstrained Stage~2 & Interpretation \\
\midrule
Ever Critical Breach
& 0.000
& 0.000
& No observed critical breach for either selected policy under the replay protocol \\

Persistent Critical Breach
& 0.000
& 0.000
& No observed persistent critical compromise for either selected policy \\

Mean Critical Dwell
& 0.000
& 0.000
& Critical compromise is absent in the evaluated replays \\

High-Confidence Critical Risk
& 0.000
& 0.000
& No high-confidence critical-risk replay is observed \\

Tier~0 Safe
& 0.000
& 0.010
& Strictly safe trajectories are rare because near-miss exposure remains visible \\

Tier~1 Near-Miss
& 1.000
& 0.990
& Both selected policies remain near-miss dominated rather than risk-free \\

Tier~2 Transient Critical Exposure
& 0.000
& 0.000
& No transient critical breach is observed \\

Tier~3 Persistent Critical Exposure
& 0.000
& 0.000
& No persistent critical breach is observed \\
\bottomrule
\end{tabular}
}
\end{table*}

\begin{table*}[t]
\centering
\small
\caption{Process-level semantic audit for selected Stage~2 policies.
Lower values indicate fewer questionable post-foothold response patterns.}
\label{tab:rq3-process-audit}
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.08}
\resizebox{\textwidth}{!}{%
\begin{tabular}{p{0.30\linewidth}ccp{0.34\linewidth}}
\toprule
Metric & DA-CPSL & Unconstrained Stage~2 & Interpretation \\
\midrule
Questionable Action During Critical State
& 0.000
& 0.000
& Not triggered because critical states are not reached \\

Missed Immediate Critical Response
& 0.000
& 0.000
& Not triggered because critical states are not reached \\

Post-Foothold Drift
& 0.002
& 0.338
& DA-CPSL substantially reduces drift after enterprise foothold \\

Repeated Low-Value Deception Loop
& 0.000
& 0.254
& DA-CPSL avoids repeated low-value deception patterns \\

Pre-Critical Restoration Rate
& 1.000
& 1.000
& Both selected policies use restoration in pre-critical containment \\

Pre-Critical Deception Rate
& 0.000
& 0.000
& Deception is not the dominant pre-critical response in the selected policies \\

Pre-Critical Critical-Path Focus
& 1.000
& 1.000
& Both policies focus on critical-path containment when pre-critical exposure is present \\
\bottomrule
\end{tabular}
}
\end{table*}

The audit reinforces the main-text interpretation. Both selected
policies are breach-free under the replay protocol, but DA-CPSL exhibits
more disciplined post-foothold behavior. The result supports
pre-critical containment under the evaluated protocol, not elimination of
risk.

\subsection{Per-Seed Paired Semantic Casebooks}
\label{app:per-seed-casebooks}

The paired casebooks compare selected DA-CPSL and Unconstrained Stage~2
policies for each main seed. Each casebook summarizes the first
meaningful response after enterprise foothold, the response pattern in
the pre-critical window, and the process-level audit interpretation. We
avoid policy checkpoint names and file paths; the casebooks are reported
at the semantic behavior level.

\begin{table*}[t]
\centering
\small
\caption{Per-seed paired semantic casebooks. The table reports behavior
at the semantic level rather than implementation-specific policy
identifiers.}
\label{tab:app-per-seed-casebooks}
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.10}
\resizebox{\textwidth}{!}{%
\begin{tabular}{cp{0.27\linewidth}p{0.27\linewidth}p{0.28\linewidth}}
\toprule
Seed & DA-CPSL Selected Policy & Unconstrained Stage~2 Selected Policy & Interpretation \\
\midrule
0007
& Early restoration-oriented response after enterprise foothold; no observed critical breach
& Breach-free endpoint but higher post-foothold drift and occasional low-value response loops
& Same breach label, but DA-CPSL shows more disciplined recovery behavior \\

0011
& Restores the compromised enterprise host shortly after foothold and avoids critical compromise
& Uses an early deception-oriented response before recovery becomes dominant
& DA-CPSL responds more directly to foothold containment \\

0019
& Restoration-oriented response with no post-foothold drift or repeated low-value deception loop
& Breach-free endpoint but decoy-first behavior and elevated post-foothold drift
& DA-CPSL reduces questionable process behavior across seeds \\
\bottomrule
\end{tabular}
}
\end{table*}

Taken together, the paired casebooks show that endpoint breach labels
alone are insufficient for evaluating autonomous defense. Two selected
policies can both be breach-free while differing in response timing,
target focus, and post-foothold discipline.

\section{Objective-Level Ablation Details}
\label{app:objective-ablation}

\subsection{Protocol-Matched Three-Objective vs. Four-Objective Comparison}
\label{app:objective-ablation-protocol}

We compare a three-objective Stage~2 variant with the four-objective
formulation used by DA-CPSL. Because the objective counts differ, the
set-level comparison first projects both methods into the shared
three-dimensional space over Security Effectiveness, Business Continuity,
and Defense Cost. Operational assignment and semantic audit are then
evaluated under the current four-objective protocol.

\begin{table*}[t]
\centering
\small
\caption{Projected archive-quality summary for the objective-level
ablation. The four-objective archive is projected into the shared
three-objective space before computing the projected metrics.}
\label{tab:rq4-objective-projected-set}
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.08}
\resizebox{0.85\textwidth}{!}{%
\begin{tabular}{p{0.34\linewidth}ccp{0.28\linewidth}}
\toprule
Metric & Three-Objective Stage~2 & Four-Objective Stage~2 & Direction \\
\midrule
Projected Hypervolume
& 2.837
& 5.590
& Higher is better \\

Projected Expected Utility
& -175.402
& -55.918
& Higher is better \\

Coverage Ratio
& 0.283
& 0.557
& Higher is better \\

Distinct Assigned Policies
& 3.667
& 3.000
& Diagnostic \\
\bottomrule
\end{tabular}
}
\end{table*}

The projected comparison shows that adding Critical-Asset Safety does
not merely improve an additional objective while sacrificing the original
three-objective trade-off surface. Even after projection into the shared
space, the four-objective archive has higher projected hypervolume,
higher expected utility, and higher coverage.

\begin{table*}[t]
\centering
\small
\caption{Semantic audit for the protocol-matched objective-level
ablation. Lower values are better for critical-breach indicators,
critical dwell, and questionable-response rules.}
\label{tab:rq4-objective-semantic-full-audit}
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.08}
\resizebox{\textwidth}{!}{%
\begin{tabular}{p{0.30\linewidth}ccp{0.34\linewidth}}
\toprule
Metric & Three-Objective Stage~2 & Four-Objective Stage~2 & Interpretation \\
\midrule
Ever Critical Breach
& 0.987
& 0.000
& Explicit Critical-Asset Safety removes observed critical breach under the protocol \\

Persistent Critical Breach
& 0.646
& 0.000
& Four-objective training avoids persistent critical compromise \\

Mean Critical Dwell
& 54.744
& 0.000
& Three-objective policies spend many steps in critical compromise \\

High-Confidence Critical Risk
& 0.983
& 0.000
& Three-objective selected policies show high-confidence critical-risk behavior \\

Tier~0 Safe
& 0.000
& 0.000
& Neither method is risk-free; near-miss behavior remains relevant \\

Tier~1 Near-Miss
& 0.012
& 1.000
& Four-objective policies move failures into pre-critical near-miss trajectories \\

Tier~2 Transient Critical Exposure
& 0.342
& 0.000
& Three-objective policies frequently reach transient critical exposure \\

Tier~3 Persistent Critical Exposure
& 0.646
& 0.000
& Three-objective policies frequently reach persistent critical compromise \\
\bottomrule
\end{tabular}
}
\end{table*}

\begin{table*}[t]
\centering
\small
\caption{Process-level audit for the objective-level ablation. Lower
values are better for questionable-response rules.}
\label{tab:rq4-objective-process-audit}
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.08}
\resizebox{\textwidth}{!}{%
\begin{tabular}{p{0.30\linewidth}ccp{0.34\linewidth}}
\toprule
Metric & Three-Objective Stage~2 & Four-Objective Stage~2 & Interpretation \\
\midrule
Questionable Action During Critical State
& 0.983
& 0.000
& Three-objective policies frequently act while already in critical compromise \\

Missed Immediate Critical Response
& 0.077
& 0.000
& Four-objective policies avoid delayed critical response under the protocol \\

Post-Foothold Drift
& 0.675
& 0.002
& Four-objective policies sharply reduce drift after enterprise foothold \\

Repeated Low-Value Deception Loop
& 0.471
& 0.000
& Four-objective policies remove repeated low-value deception behavior \\

Pre-Critical Restoration Rate
& 0.413
& 1.000
& Four-objective policies emphasize restoration during pre-critical containment \\

Pre-Critical Deception Rate
& 0.489
& 0.000
& Three-objective policies rely more heavily on deception during pre-critical exposure \\

Pre-Critical Critical-Path Focus
& 0.295
& 1.000
& Four-objective policies focus more directly on critical-path containment \\
\bottomrule
\end{tabular}
}
\end{table*}

These results support the representation claim in the main text:
Critical-Asset Safety changes which operating points are treated as
acceptable. Without the fourth objective, selected policies can appear
competitive in conventional objective-space metrics while repeatedly
entering critical compromise under the current semantic audit.

\section{Supporting Robustness: Held-Out Attacker Shift}
\label{app:attacker-shift}

We performed an appendix-only robustness probe that trains under the
default attacker process and reevaluates the learned archives under a
held-out attacker process. This experiment is not treated as an
additional main evaluation question because the paper is not an
attacker-generalization study. Instead, it probes a boundary of the
current method family.

\begin{table*}[t]
\centering
\small
\caption{Held-out attacker-shift summary. Training uses the default
attacker process; evaluation uses a held-out attacker process with the
same shared-threshold assignment protocol.}
\label{tab:attacker-shift-summary}
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.08}
\resizebox{0.85\textwidth}{!}{%
\begin{tabular}{p{0.30\linewidth}cccp{0.25\linewidth}}
\toprule
Method & Feasible Rate & Mean Violation & Final Critical Hosts & Interpretation \\
\midrule
DA-CPSL
& 0.000
& 6.994
& 0.000
& No acceptable assignment under the shifted attacker process \\

Stage-1 Archive
& 0.017
& 8.074
& 0.000
& Minimal feasible assignment rate under attacker shift \\

Unconstrained Stage~2
& 0.025
& 6.409
& 0.000
& Slightly higher feasible rate but still not operationally reliable \\

Weighted-Sum PPO
& 0.000
& 99.314
& 0.608
& High violation and nonzero final critical compromise \\
\bottomrule
\end{tabular}
}
\end{table*}

The attacker-shift probe is mainly cautionary. All archive-based methods
degrade sharply under the held-out attacker process, and DA-CPSL returns
no acceptable assignments under the shifted protocol. This reinforces
the scope boundary of the main paper: broader attacker variation remains
an open robustness problem rather than a solved property of the current
method family.

\section{Artifact and Reproducibility Notes}
\label{app:artifact-notes}

The artifact package should include the training configurations,
evaluation configurations, semantic audit definitions, table-generation
scripts, figure-generation scripts, and per-seed result files used to
produce the main and appendix results. It should also include the replay
casebook artifacts used for paired semantic analysis.

For double-anonymous review, the artifact should be hosted anonymously
and should not expose author names, institution names, public repository
identities, commit history, or identifying access logs. If any
third-party environment component cannot be mirrored anonymously due to
license or packaging constraints, the artifact should explain the
restriction and provide setup instructions sufficient to reproduce the
methodology and evaluate the reported results.



\subsection{Per-Seed Main Results}
\label{app:per-seed-main-results}

Because the main experiments use three seeds, we additionally report the
primary archive-quality and operational-assignment metrics at the
per-seed level. These appendix tables are intended to test whether the
main improvements are driven by a single favorable run or instead remain
directionally consistent across seeds.

\begin{table*}[t]
\centering
\small
\caption{Per-seed archive-quality breakdown for the three main seeds.
The table reports hypervolume and expected utility under the shared
four-objective evaluation protocol.}
\label{tab:app-per-seed-archive-quality}
\resizebox{\textwidth}{!}{%
\begin{tabular}{@{}llrr@{}}
\toprule
Seed & Method & Hypervolume ($\times 10^6$) & Expected Utility \\
\midrule
0007 & Constraint-Aware Stage-2 & 17.147 & -46.785 \\
0007 & Stage-1 Archive & 16.903 & -46.809 \\
0007 & Weighted-Sum & 6.705 & -136.944 \\
0007 & Preference-Conditioned PPO & 2.467 & -154.838 \\
0011 & Constraint-Aware Stage-2 & 27.255 & -36.658 \\
0011 & Stage-1 Archive & 25.894 & -42.718 \\
0011 & Weighted-Sum & 5.156 & -171.152 \\
0011 & Preference-Conditioned PPO & 5.314 & -175.946 \\
0019 & Constraint-Aware Stage-2 & 24.327 & -42.315 \\
0019 & Stage-1 Archive & 23.718 & -45.113 \\
0019 & Weighted-Sum & 5.654 & -176.605 \\
0019 & Preference-Conditioned PPO & 3.258 & -173.017 \\
\bottomrule
\end{tabular}
}
\end{table*}

\begin{table*}[t]
\centering
\small
\caption{Per-seed operational-assignment breakdown for the three main
seeds. The table reports feasible-assignment rate, mean violation, final
critical compromised hosts, and critical-impact count under the shared
acceptance protocol.}
\label{tab:app-per-seed-operational-assignment}
\resizebox{\textwidth}{!}{%
\begin{tabular}{@{}llrrrr@{}}
\toprule
Seed & Method & Feasible Rate & Violation & Final Critical Hosts & Critical Impacts \\
\midrule
0007 & Constraint-Aware Stage-2 & 0.025 & 0.278 & 0.000 & 0.000 \\
0007 & Stage-1 Archive & 0.000 & 0.321 & 0.000 & 0.000 \\
0007 & Weighted-Sum & 0.000 & 91.092 & 0.625 & 6.050 \\
0007 & Lagrangian PPO & 0.000 & 91.866 & 0.775 & 4.350 \\
0007 & Unconstrained Stage-2 & 0.350 & 1.816 & 0.000 & 0.000 \\
0011 & Constraint-Aware Stage-2 & 0.925 & 0.004 & 0.000 & 0.000 \\
0011 & Stage-1 Archive & 0.350 & 3.754 & 0.000 & 0.000 \\
0011 & Weighted-Sum & 0.000 & 102.630 & 0.875 & 4.925 \\
0011 & Lagrangian PPO & 0.000 & 103.041 & 0.900 & 1.000 \\
0011 & Unconstrained Stage-2 & 0.525 & 2.600 & 0.000 & 0.000 \\
0019 & Constraint-Aware Stage-2 & 0.975 & 0.000 & 0.000 & 0.000 \\
0019 & Stage-1 Archive & 0.575 & 1.856 & 0.000 & 0.000 \\
0019 & Weighted-Sum & 0.000 & 86.909 & 0.975 & 1.000 \\
0019 & Lagrangian PPO & 0.000 & 91.916 & 0.975 & 1.125 \\
0019 & Unconstrained Stage-2 & 0.500 & 1.967 & 0.000 & 0.000 \\
\bottomrule
\end{tabular}
}
\end{table*}

Across all three seeds, DA-CPSL remains the strongest or near-strongest
archive-quality method, showing that the main-table gains are not driven
by a single run. Per-seed deployment metrics likewise show that
DA-CPSL's feasible-assignment advantage and violation reduction persist
across seeds, rather than emerging from one unusually favorable seed.
The purpose of these tables is to confirm directional stability, not to
claim that all seeds produce identical magnitudes. The per-seed results
therefore strengthen the interpretation that the aggregate gains reflect
a repeatable trend under the stated protocol.

\subsection{Raw Pareto Assignment vs. Acceptable Pareto Assignment}
\label{app:raw-vs-acceptable-assignment}

\begin{table*}[t]
\centering
\small
\caption{Raw Pareto assignment versus acceptable Pareto assignment on the DA-CPSL Stage~2 archive. Feasible-assignment rate is measured over preference-grid assignments; violation and critical-risk audit metrics are assignment-weighted over selected policies.}
\label{tab:app-raw-vs-acceptable-assignment}
\resizebox{\textwidth}{!}{%
\begin{tabular}{@{}lrrrrr@{}}
\toprule
Assignment Rule & Expected Utility & Feasible Assign. Rate & Mean Violation & Final Critical Hosts & Critical Impact Count \\
\midrule
Raw Pareto Assignment & -41.919 & 0.679 & 1.189 & 0.000 & 0.000 \\
Acceptable Pareto Assignment & -43.706 & 1.000 & 0.000 & 0.000 & 0.000 \\
\bottomrule
\end{tabular}%
}
\end{table*}

This mechanism ablation uses the same DA-CPSL Stage~2 archive, the same
four-objective preference grid, and the same operational thresholds as
the main evaluation. The only change is the assignment rule: Raw Pareto
Assignment selects directly from the objective-space Pareto set, whereas
Acceptable Pareto Assignment first applies the operational acceptance
filter and then assigns from the acceptable Pareto set.

The raw rule preserves the highest preference utility, but it can assign
policies that are Pareto-optimal only in objective space while violating
the hard business or cost limits. In contrast, the acceptable rule
raises the feasible-assignment rate from \(0.679\) to \(1.000\) and
reduces mean violation from \(1.189\) to \(0.000\), at the cost of a
small reduction in expected utility. This appendix-only comparison does
not introduce a new training result and does not replace
Table~\ref{tab:operational-assignment-performance}; it isolates the
assignment-rule mechanism and directly supports the design motivation in
Section~\ref{subsec:acceptable-pareto-assignment}.

\subsection{Operational-Threshold Sensitivity}
\label{app:threshold-sensitivity}

\begin{table*}[t]
\centering
\small
\caption{Operational-threshold sensitivity under looser, default, and stricter business/cost limits. Feasible-assignment rate and mean violation are computed over preference-grid assignments from each method's raw Pareto candidates.}
\label{tab:app-threshold-sensitivity}
\resizebox{\textwidth}{!}{%
\begin{tabular}{@{}llrr@{}}
\toprule
Threshold Profile & Method & Feasible Assign. Rate & Mean Violation \\
\midrule
Looser & Constraint-Aware Stage-2 & 0.719 & 0.849 \\
Looser & Stage-1 Archive & 0.716 & 0.859 \\
Looser & Weighted-Sum & 0.000 & 101.242 \\
Looser & Lagrangian PPO & 0.000 & 95.034 \\
Looser & Unconstrained Stage-2 & 0.746 & 0.768 \\
\addlinespace
Default & Constraint-Aware Stage-2 & 0.679 & 1.189 \\
Default & Stage-1 Archive & 0.667 & 1.214 \\
Default & Weighted-Sum & 0.000 & 102.086 \\
Default & Lagrangian PPO & 0.000 & 95.825 \\
Default & Unconstrained Stage-2 & 0.746 & 1.069 \\
\addlinespace
Stricter & Constraint-Aware Stage-2 & 0.642 & 1.700 \\
Stricter & Stage-1 Archive & 0.304 & 2.217 \\
Stricter & Weighted-Sum & 0.000 & 104.273 \\
Stricter & Lagrangian PPO & 0.000 & 97.625 \\
Stricter & Unconstrained Stage-2 & 0.692 & 1.509 \\
\bottomrule
\end{tabular}%
}
\end{table*}

We also test whether the operational-assignment conclusion depends on a
single hand-picked threshold profile. Using the same official
four-objective artifacts, seeds, and preference grid, we evaluate three
business/cost profiles derived from the Table~B calibration set:
looser \(q=0.10\), default \(q=0.25\), and stricter \(q=0.50\); the
default profile exactly matches the shared thresholds used in the main
evaluation. For each profile, the assignment rule first selects from
each method's raw Pareto candidates by preference utility and then
audits whether the selected objective vector satisfies the corresponding
business and cost limits.

As expected, tighter thresholds reduce feasible-assignment rates and
increase mean violation for every method. DA-CPSL moves from
\((0.719, 0.849)\) under the looser profile to \((0.679, 1.189)\) at
the default profile and \((0.642, 1.700)\) under the stricter profile,
showing gradual degradation rather than a collapse at one operating
point. Stage-1 Only is more sensitive to tightening, dropping from
\(0.716\) feasible-assignment rate under the looser profile to
\(0.304\) under the stricter profile, while Unconstrained Stage~2
retains slightly higher feasibility but also accumulates more violation
as the limits tighten, from \((0.746, 0.768)\) to \((0.692, 1.509)\).
Weighted-Sum and Lagrangian PPO remain infeasible across all three
profiles, with mean violation above \(95\), so the appendix conclusion
is robustness of the relative deployment trend rather than a new
training or replay result. It isolates threshold sensitivity at the
candidate/objective assignment level.

\subsection{Zero-Observed Critical-Event Confidence Bounds}
\label{app:zero-event-confidence-bounds}

\begin{table*}[t]
\centering
\small
\caption{Zero-observed critical-event confidence bounds under the finite semantic replay protocol. Bounds are one-sided Clopper--Pearson upper bounds at 95\% confidence and do not imply zero risk.}
\label{tab:app-zero-event-confidence-bounds}
\resizebox{\textwidth}{!}{%
\begin{tabular}{@{}llrrr@{}}
\toprule
Event & Method & Observed Events / Episodes & Observed Rate & 95\% CP Upper Bound \\
\midrule
Ever Critical Breach & Constraint-Aware Stage-2 & 0/120 & 0.00\% & 2.47\% \\
Persistent Critical Breach & Constraint-Aware Stage-2 & 0/120 & 0.00\% & 2.47\% \\
Any Critical Impact & Constraint-Aware Stage-2 & 0/120 & 0.00\% & 2.47\% \\
Any Final Critical Host Compromised & Constraint-Aware Stage-2 & 0/120 & 0.00\% & 2.47\% \\
Ever Critical Breach & Stage-1 Archive & 0/120 & 0.00\% & 2.47\% \\
Persistent Critical Breach & Stage-1 Archive & 0/120 & 0.00\% & 2.47\% \\
Any Critical Impact & Stage-1 Archive & 0/120 & 0.00\% & 2.47\% \\
Any Final Critical Host Compromised & Stage-1 Archive & 0/120 & 0.00\% & 2.47\% \\
Ever Critical Breach & Unconstrained Stage-2 & 0/120 & 0.00\% & 2.47\% \\
Persistent Critical Breach & Unconstrained Stage-2 & 0/120 & 0.00\% & 2.47\% \\
Any Critical Impact & Unconstrained Stage-2 & 0/120 & 0.00\% & 2.47\% \\
Any Final Critical Host Compromised & Unconstrained Stage-2 & 0/120 & 0.00\% & 2.47\% \\
\bottomrule
\end{tabular}%
}
\end{table*}

The zero critical-event entries in the main audit are finite-protocol
observations, not formal guarantees of zero risk. We therefore report
one-sided Clopper--Pearson upper confidence bounds for event-style
semantic replay outcomes with zero observed events. For each method and
event, the analysis pools the three official seeds, giving
\(3\times 40=120\) semantic replay episodes under the same Table~B
protocol.

For DA-CPSL, zero observed ever-critical breaches, persistent critical
breaches, critical-impact events, and final critical compromised hosts
yield a \(95\%\) upper bound of \(2.47\%\) on the corresponding
per-episode event rate under this replay protocol. Stage-1 Only and
Unconstrained Stage~2 have the same zero-observed-event bound for these
audit events, whereas Weighted-Sum and Lagrangian PPO show nonzero
critical-event counts and are therefore retained only in the
machine-readable audit outputs for this analysis. This supports the
interpretation ``breach-free under the replay protocol, but not
risk-free.''







\end{document}
